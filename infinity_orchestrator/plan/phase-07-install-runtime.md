# Phase 7 — Step 5 install runtime + Reinstall + import-code auto-write + install concurrency + rail-nav lock

**Status:** SHIPPED — full audit record: [`archive/phase-07-install-runtime.md`](archive/phase-07-install-runtime.md).

**Commit anchors:** Run 1 `5b8a60e` "Phase 7 Run 1: Step-5 runtime spine + workspace chrome + Previous-lock (P7.T1/T2/T8)" → Run 2 `e02dbe9` "install-start + concurrency + C5 rail-lock + flag policies + statusbar (P7.T3/T9/T9b/T16/T14)" → Run 3 `0b58cc9` "post-install — success banner + actions + registry flip + share dialog (P7.T4/T5/T6/T7/T12/T13)" → Run 4a `4e1d064` "live download/extract/import pipeline + per-install dirs + content-addressed staging (P7.T17)" → Run 4b `dabc749` "real §4.4 install screen + Reinstall flow + import-code matrix (P7.T15/T10/T11)" → §13.13 fix-run `7e0e414` → `accb66f` "Install-Modlist-paste registry lifecycle + §13.13 on the pipeline path — PHASE 7 COMPLETE". Subsequent Phase-7 fix arc (5 runs, user-authorized 2026-05-18): `7339e4e` Run 1 (THE AMENDMENT — `weidu_component_logs` back in destination after upstream `a38e360`/merge `8df994a`), `6f7f771` Run 2 (A-1 persist held code), `1c36856` Run 3 (B per-component WeiDU logs via `sync_weidu_log_mode`, ZERO BIO), `3d2ffed` Run 4 (#1 net-new parallel streaming downloader, ZERO BIO), `837bef2` Run 5 integration proof + consolidated doc-sync, plus consolidated doc-sync `d656610` / `e444f79`. Earlier ad-hoc Phase-7 fix passes: `4a8db16` / `f84fdcb` / `ffcba48` / `a0bf735`. Post-arc user-test follow-up fix-set (Wabbajack model): `7ec83d1` DL-Run 1 (checksum-then-skip + per-archive `{size,hash}` in share code), `2747d06` DL-Run 2 (byte-accurate two-phase Downloading window), `cad9297` DL-Run 3 (§4.4 success banner restored), `e444f79` consolidated post-arc doc-sync. **PR #5 install-screen bundled-fixes arc (merged `3d8287d`, 5 commits on `fix/install-screen-bundled-fixes`):** `ed60288` DL v4 + Step-5 bundle (Cancel resets / hardlink dedupe / Hashing classification / snapshot reset / final-asset bar pin / Step-5 console clip), `b939aae` Create/Fork pipeline wiring + destination-prep widening (new `install_runtime::destination_prep` + `fork_pipeline_arm`), `3164f17` upstream cherry-pick by Born2BSalty (BIO console wrap `Wrap` + `break_anywhere=true` + `LayoutJob`; per-install log scope at install-start; `last_rendered_nav` route reset — author credit preserved), `f640596` post-cherry-pick 4-bug sweep (hardlink-fallback `tracing::warn` + nav glyph-font selection fix + extract-pool warn-instrumentation + new `install_runtime::settings_sanitizer` to keep per-install fields out of `bio_settings.json`), `b6f7f7c` Item #5 Path-L deeper fix (dropped the "Temp" field from Settings → Paths per SPEC §13.12a "derived, never asked" + restored `step1` at install-completion edge in `reset_completed_install_runtime`). **PR #6 cleanup arc (merged `6ea7b5c`, Born2BSalty `Salty_fmt_clippy_Version_update`):** major refactor of `src/ui/install/stage_downloading.rs` (-993 LOC), new `src/ui/orchestrator/widgets/icon_button.rs` (+148 LOC), Step 2 details UI cleanup, `fork_extract_complete` gate-eval `tracing::info!` instrumentation (Item #4 diagnostic — logs every flag + step2 condition the predicate inspects), removed 4 flaky render-PNG / runtime-trace tests (egui_kittest cross-platform issues on Ubuntu CI), Cargo version `0.1.0-beta.19` → `0.1.0-Alpha.1`, CI tightened to `cargo test --all-targets --locked`. **Open follow-up — Item #4 Create-fork post-route UX (branch `xgatt/create-then-modify-fix`, single-branch arc):** **Run 1 landed `2c95df9`** — fork-route un-blocked (page_router was clearing `active_install_modlist_id` mid-fork-extract; skipped during `CreateStage::ForkDownload`). **Run 2 landed `b284525`** — apply-imported-WeiDU-log selection on Workspace landing + suppress auto-opened Update Check popup + persist `scratch_mods_folder` for non-dev resume + extended `pick_resume_folder` to prefer `scratch_mods_folder` over `dev_scanned_mods_folder`. **Run 3 (uncommitted)** — three bugs from user testing: (i) **rescan-wipes-step2** — `should_reset_completed_install_route` was firing during an active fork-then-modify Workspace because `has_route_context() + clean_exit` proxies were satisfied by legitimately-populated `destination`/`import_code` strings + a carried `last_exit_code = Some(0)`; replaced with a new `PostInstallResetGate` enum flag set in `maybe_flip_to_installed_on_clean_exit` after `flip_to_installed` succeeds and cleared in `reset_completed_install_runtime`, so the reset only fires after a real install completion. (ii) **Step-4 → Step-5 edits lost** — workspace's Step-4 → Step-5 nav now calls `auto_save_step4_weidu_logs` so the user's Step 2/3 edits propagate to the per-install weidu.log files the install runner reads (mirrors legacy BIO's `NeedStep4SaveThenAdvance`). (iii) **gate-eval log spam** — `fork_extract_complete`'s diagnostic log now also requires `!scan_running && !apply_pending && !update_preview_pending` so it emits only at the terminal evaluation, not throughout the post-extract wait window. ZERO BIO source touched. Auto-update / asset-substitution concern remains deferred per user decision (Born2BSalty's thread).

**What shipped:** the existing BIO Step 5 install runtime wrapped in the new workspace chrome — the chrome **calls `bio::ui::step5::page_step5::render` directly** and wraps it in sibling rows above/below rather than reaching inside the Step-5 module tree. Adds (as net-new orchestrator-owned components per the CRITICAL DIRECTIVE): pre-install chrome row, post-install chrome row with C3-clean-exit success banner + `Share import code` / `Return to Home` / `Open install folder` actions (positioned per H9 immediately above BIO's panel), the workspace nav lock once Install is clicked, the `in-progress` → `installed` registry transition gated on the C3 triple (`install_running == false && last_exit_code == Some(0) && last_install_failed == false`), the install concurrency policy gate, the **C5 rail-navigation lock** during in-flight installs, Install Modlist stage 4 wiring, the Reinstall flow routing through Install Modlist preview with overwrite-install forced, the `modlist-import-code.txt` auto-write on install start across every entry point (SPEC §13.13), and **P7.T17 per-install directory derivation + content-addressed archive staging + import → auto-build pipeline drive that binds the Phase-5 §4.3 chassis live (SPEC §13.12a)**. The fix arc reverted Fix-A (per-component WeiDU log dir back in the destination after upstream `a38e360` removed the no-space preflight), persisted the user-pasted share code unchanged with only the `allow_auto_install` bit flipped via the net-new `share_export::set_allow_auto_install` (A-1 — `pack_meta` cannot regenerate when `state.step3` is empty on the paste path), implemented per-component WeiDU logging via BIO's existing `pub fn sync_weidu_log_mode` read-only (B — no seventh carve-out; mechanism is the `weidu_log_mode` `log <folder>` token, not a `-u` CL flag), and replaced BIO's serial download worker with a net-new parallel `stream_downloader` while reusing BIO's extract unchanged (#1). The post-arc Wabbajack-model follow-up added per-archive `{size,hash}` in the share code (new schema-additive `archive_meta` sibling, orchestrator-owned), install-time checksum-then-skip + post-download verify, a byte-accurate two-phase Downloading window (separate Download then Extract overall bars), and restored the §4.4 success banner.

**Task IDs (full detail in archive):** P7.T1, P7.T2, P7.T3, P7.T4, P7.T5, P7.T6, P7.T7, P7.T8, P7.T9, P7.T9b, P7.T10, P7.T11, P7.T12, P7.T13, P7.T14, P7.T15, P7.T16, P7.T17.
