# Phase 8 — Per-Modlist Download-Source Ownership (PR-1: correctness core)

**Status:** PLANNED — not started.

---

## Review round 1 — fixes applied (2026-06-08)

Adversarial review of the first plan draft surfaced six findings + one test hazard. Each is now reflected in the tasks/tables below (grounded against live code at review time):

- **F1 (Critical) — Import isolation was inert for plain paste-and-install.** The paste-and-install import runs at `stage_downloading.rs:699` (`finish_pipeline_arm_after_destination_prep` → `prepare_install_dirs_and_maybe_import` → `import_modlist_share_code`, `auto_build_driver.rs:39`) **before** the modlist id is minted at `stage_downloading.rs:726` → `install_modlist_registration.rs:128`. So at import time the ambient is `None` and the pin write fell back to **global**, re-introducing the bleed and failing acceptance §10.5. **Fix (chosen mechanism = option (a)): mint the id + set the ambient in the redesign-owned `finish_pipeline_arm_after_destination_prep` BEFORE `prepare_install_dirs_and_maybe_import`** for the share-code-consuming arm; T7 stays ambient-only across every import path. See PMD.T2 (new seam **2d**) and PMD.T7. (The FORK arm is already correct — `fork_pipeline_arm.rs:120` sets the id before import.)
- **F2 (High) — "My default" edit promoted a modlist pin into global.** `load_user_mod_download_source_block` seeds via `load_mod_download_sources().resolve_source` (`mod_downloads.rs:221`) — the FULL 3-tier value — so with a modlist active, opening "My default" pre-filled (and saving wrote) the modlist's pin into global. **Fix:** per §5, the `GlobalDefault` seed resolves WITHOUT the per-modlist overlay (a two-tier loader variant). See PMD.T3 / PMD.T4.
- **F3 (High) — Export serializer via `source_to_editor_block` was lossy / mis-emitting.** `source_to_editor_block` (`mod_downloads.rs:774-848`) hardcodes `type = "github"`, emits `repo` only if `github.is_some()`, drops `config_files` + `source_default`, and emits `exact_github` as **repeated scalar lines** (`:785-790`) → a TOML duplicate-key error for any multi-element `exact_github`, aborting the whole share export. **Fix:** PMD.T6 gets a dedicated export serializer (`exact_github` as a TOML array; correct `repo` for GitHub, plain `url` for non-GitHub `type = "url"`/`"page"` sources; round-trip-validated). See PMD.T6.
- **F4 (High) — Resolved-set bake must be gated on an active modlist.** Per §6, with no active modlist (the `BIO_legacy` binary, which reaches the *shared* export button at `content_install_row_step5.rs:236` with the ambient unset) today's verbatim-copy path is preserved byte-identical; in the redesign workspace flow that same callsite runs ambient-**set** and bakes the resolved set. The gate keys on the **ambient**, not the callsite. See PMD.T6.
- **F5 (Medium) — Empty / no-resolvable-source export.** Skip a mod with no resolvable source; an empty `source_overrides` is allowed (the importer already filters empty text). See PMD.T6.
- **F6 (Low) — `state_step2.rs` field type.** `Step2State<Flag = bool>` is generic; the new destination field is a **concrete enum**, not a `Flag`. See PMD.T4.
- **Test-isolation hazard.** The ambient is a process-global; `cargo test` runs in-process, so a test that sets it leaks into later tests and a panicking test leaves it set. Every ambient-touching test now resets the ambient via an RAII drop-guard. See Test plan.

**Review round 2 — focused re-review of the round-1 fixes (2026-06-08).** All six fixes confirmed substantively correct and code-grounded — F1's no-double-mint (via destination-dedup) and F4's ambient gate were verified against the reinstall and shared-export paths. Only Low/Medium items surfaced; all folded in: (1) **F6 needs `Eq`** — `Step2Action` derives `Eq`, so `ModSourceEditDestination` must too, else the new field breaks the variant's derive (compile-correctness). (2) **F1 orphan-`InProgress` on import failure** — minting before the import means a failed import would leave an orphan registry entry; PMD.T2 now rolls back a freshly-minted entry on the import `Err` arm. (3) **Mandatory test-lock** — the process-global ambient + multithreaded `cargo test` + ~20 pre-existing loader/export test files require a shared `Mutex` test-lock around every ambient-touching test (the drop-guard alone doesn't prevent cross-thread interleave). (4) **F4 wording** — `content_install_row_step5.rs:236` is a *shared* callsite (BIO reaches it ambient-set; only `BIO_legacy` reaches it ambient-unset); the gate keys on the ambient, not the callsite. (5) **F3 note** — non-selector fields inherit from the default tier via the field-wise overlay, so the serializer emits only pin selectors + source identity.

**Authority:** `infinity_orchestrator/SPEC-per-modlist-download-sources.md` (focused sub-spec, user-locked 2026-06-08) is the gospel for this work; `SPEC.md §1` CRITICAL DIRECTIVE governs every BIO-source edit. This plan implements that focused spec in full (its §3 data model, §4 reads/ambient, §5 write dropdown, §6 export, §7 import, §8 legacy preservation, §9 carve-out #16 surface, §10 acceptance, §11 deferral) and never deviates.

**Relationship to the existing `P8.T14` (`§13.12b`) task.** The focused sub-spec re-scopes and supersedes the **mod-source slice** of the older `P8.T14` design in `phase-08-popup-reskins-polish.md`. Where T14.4 proposed an *orchestrator-side interception* of the Set-Source save and an *export-reads-redirect* with `export_modlist_share_code` "composed unchanged", the user's 2026-06-08 locked decisions chose instead: **ambient resolution + a third loader pass inside BIO** (§4) and a **carve-out #16 edit to `export_modlist_share_code`** to bake the full resolved set (§6). The newer locked sub-spec wins. This plan covers only the **download-source** overlay (`mod_downloads_user.toml`); the compat-rules and installed-source-refs overlays from §13.12b / T14.3–T14.5 remain future work and are **out of scope** here (see Out-of-scope). Reconciling the two plan docs is a Doc-sync item, not a code task.

---

## Summary

A mod's pinned download source/version lives in a **single global** `mod_downloads_user.toml`, shared across every modlist, re-read on every resolution. This bleeds across modlists in three confirmed places: export bakes the last-pinned-anywhere version into share codes (primary, user-facing), manual workspace resolution picks up another modlist's pin, and import clobbers the user's global defaults. (Focused spec §1.)

PR-1 makes a mod's pin **ownable at the modlist level** via a sparse **third overlay tier** (`modlists/<id>/mod_downloads_user.toml`) layered field-wise on top of the existing app-default → global-user merge, resolved everywhere through an **ambient active-modlist** the orchestrator sets. Writes gain a **destination dropdown** ("My default" / "For this modlist"). Export **bakes the full resolved set** this modlist installs. Import **lands pins in the imported modlist's own file**. The legacy `BIO_legacy` binary never sets the ambient, so its read/write/export/import are byte-for-byte unchanged.

**One-seam elegance:** 15 call sites resolve sources via the no-arg `bio::app::mod_downloads::load_mod_download_sources()`. Making *that loader* ambient-aware makes every one of them modlist-aware at once — install pipeline, manual update-check, preview, log-apply, export. The plan centers the read design on that single seam.

## What ships (PR-1)

1. The per-modlist file `modlists/<id>/mod_downloads_user.toml` as a third, sparse, field-wise overlay tier (focused §3).
2. An **ambient active-modlist override path** the loader consults; set/cleared by the orchestrator on workspace open, install start, and return to Home; never set by `BIO_legacy` (focused §4, §8).
3. A **target-path capability** on the BIO source-block load/save so a write can target the per-modlist file (focused §5, §9.2).
4. The Updates-popup **"Edit Source" → destination dropdown** ("My default" / "For this modlist"), with the chosen destination carried open→save (focused §5, §9.4).
5. **Export** baking the full resolved set, modlist-scoped — replacing the verbatim global-file copy **when a modlist is active**; with no active modlist (legacy `BIO_legacy` export) today's verbatim copy is preserved byte-identical (focused §6, §9.5; F4). A dedicated round-trippable serializer (not the lossy editor helper) emits the bake (F3).
6. **Import** writing baked pins into the new modlist's per-modlist file, leaving the global file untouched (focused §7, §9.6).
7. Carve-out #16 formalized into `SPEC.md §1`; HANDOFF + §13.12b cross-refs updated (Doc-sync owed).

## Out-of-scope (do not build; do not preclude)

- **PR-2 entirely** (focused §11): version-mismatch detection between pinned and extracted-on-disk version; the per-mod re-download+re-extract remediation; any per-modlist tracking of the extracted version. PR-1 establishes the pin (source-of-truth) PR-2 compares against; build none of it, architecturally preclude none of it.
- **The compat-rules-user and installed-source-refs per-modlist overlays** (§13.12b / P8.T14.3–T14.5). The focused spec §3 names only `mod_downloads_user.toml`. The export/import companion **installed-refs** payload stays on its **current global path** except where §6 scopes its *content* to this modlist's selections (which are already per-`WizardState`/per-modlist via `step2.selected_source_ids`) — no new per-modlist installed-refs *file* is created here. See Judgment call J3.
- **`src/core/app/modlist_pack/export.rs`** — a separate diagnostic-bundle exporter that also copies `mod_downloads_user`. It is **not wired into any live path** (grep: referenced only by itself + an archived plan doc). The focused spec §6 names only `export_modlist_share_code`. Explicitly out of scope; do not edit.
- The `P8.T14` in-memory workflow refactor (`CurrentWorkflow`, derive-step1, settings-sanitizer retirement) — unrelated to this feature.

## Dependencies

- Phases 1–7 shipped + merged; Phase 8 visual-fidelity sweep (T1–T13) state is irrelevant to this work.
- The redesign-owned active-modlist identity already exists: `NavDestination::Workspace { modlist_id: Some(id) }` (`src/ui/orchestrator/nav_destination.rs`), `OrchestratorApp.active_install_modlist_id` (`orchestrator_app.rs:282`), and the per-modlist data dir helper `bio::registry::store_workspace::modlist_data_dir(id)` (`store_workspace.rs:15`) returning `<config_dir>/bio/modlists/<id>/`.
- The orchestrator-owned per-frame nav seam `src/ui/orchestrator/page_router.rs::render` (observes `previous_nav` vs `rendered_nav`).
- The orchestrator-owned install-start seam `src/install_runtime/start_hooks.rs::write_install_start_artifacts(modlist_id, ...)` and `pack_meta` composition (`src/registry/share_export.rs:68`).

---

## File inventory

### New files (redesign-owned, net-new — no carve-out)

| File | What |
|------|------|
| `src/install_runtime/active_modlist_source_path.rs` | Orchestrator-owned helper that computes the per-modlist override path (`modlist_data_dir(id).join("mod_downloads_user.toml")`) and the set/clear discipline against the BIO ambient setter. Pure path + orchestration glue; reuses `modlist_data_dir`. Holds the set-on-nav, set-on-install-start, clear-on-home logic invoked from the seams below. |

> **Decision-order note (directive option 2 vs 3).** The *ambient setter + loader pass + target-path capability + export/import* all live in protected BIO and cannot be cloned without forking the resolver/store/share-code interop — exactly the "complex protected surface" carve-out #16 exists for (directive option 3). The *seam glue* that decides *which* path to set and *when* is simple orchestration over an already-public BIO setter and the already-public `modlist_data_dir` — a net-new sibling (directive option 2). Hence one small net-new file + the carve-out #16 BIO edits, and nothing more.

### Carve-out #16 BIO edits (protected source — each additive/inert on the unset-ambient / no-override path)

All line numbers verified against live code at plan time; the implementer re-confirms before editing.

| File | Function / item | Lines (verified) | Edit | Why additive / behavior-preserving when ambient unset |
|------|-----------------|------------------|------|--------------------------------------------------------|
| `src/core/app/mod_downloads.rs` | module top | new `static` + `pub(crate) fn set_active_modlist_downloads_path(Option<PathBuf>)` + `fn active_modlist_downloads_path() -> Option<PathBuf>` | additive new items | When unset (`None`), the new accessor returns `None` and every consumer below short-circuits to today's path. |
| `src/core/app/mod_downloads.rs` | `load_mod_download_sources()` | ~283–331 | After the existing default pass (~290–301) and global-user pass (~302–323), append a **third pass** that, *iff* the ambient path is `Some` AND the file exists, loads its overlays via the existing `load_source_overlays_from_path` and applies them with the same `apply_source_overlay` / `clear_other_source_defaults` field-wise logic. **Factor the existing default→global-user merge body (290–323) into a private `load_two_tier_sources()` helper and have the no-arg `load_mod_download_sources()` call it then run the third pass; the new helper IS the two-tier value (no per-modlist overlay).** | The third pass is gated on `Some(path)`; with `None` the loop body is never entered, so the returned `BTreeMap` is byte-identical to today's two-tier merge. The extracted `load_two_tier_sources()` is a pure refactor of code that already exists; its output equals today's full result on the unset-ambient path. |
| `src/core/app/mod_downloads.rs` | **new** `pub(crate) fn load_user_mod_download_source_block_global_seed(...)` *(F2)* — OR a `seed_scope: SeedScope { Resolved, GlobalOnly }` param on `load_user_mod_download_source_block` | new item near 209–230 | A loader variant whose editor pre-fill seeds from the **two-tier** resolution (`load_two_tier_sources().resolve_source(...)`) instead of the 3-tier `load_mod_download_sources()` (221). The `GlobalDefault` destination seeds via this variant; `ThisModlist` keeps the full 3-tier seed. | Purely additive: the two-tier seed equals today's seed whenever the ambient is unset (no third pass exists then), so legacy behavior is byte-identical. With an ambient set, "My default" now shows the true global value (§5) instead of leaking the modlist pin — the bug fix. |
| `src/core/app/mod_downloads.rs` | `save_user_mod_download_source_block(...)` | 232–281 | Add a target-path parameter (an explicit `&Path`, or a `destination` enum, or an `Option<PathBuf>` that defaults to `mod_downloads_user_path()`); replace the two `let path = mod_downloads_user_path();` (240) reads and the global resolver fall-throughs at 260–262 with the target path. The text-block replace/append + `toml::from_str::<ModDownloadsFile>` validation + `fs::write` logic is otherwise unchanged. | Call sites that pass the global path (or `None`) get today's exact behavior. `ensure_mod_downloads_files()` still seeds the global default/user files; the per-modlist target is created on demand by the existing append path. |
| `src/core/app/mod_downloads.rs` | `load_user_mod_download_source_block(...)` | 209–230 | Add the target-path parameter **and a seed-scope selector** (per F2/§5). Replace `let path = mod_downloads_user_path();` (216) with the target (existing-user-block lookup reads the chosen destination file). The `resolve_source` seed (221) becomes **tier-matched**: `ThisModlist` → the full 3-tier `load_mod_download_sources().resolve_source(...)` (today's behavior); `GlobalDefault` → the **two-tier** `load_two_tier_sources().resolve_source(...)` so "My default" pre-fills the true global value, never the modlist's pin. | Global-path/`GlobalDefault` callers with the ambient unset are byte-identical (the two-tier and 3-tier seeds coincide when no third pass exists). When seeding "For this modlist" with no per-modlist block yet, `existing_user_block` is `None` and the merged-source seed path (704–708) already handles that — same as a fresh global edit today. The §5 fix only changes the seed when an ambient IS set and `GlobalDefault` is chosen. |
| `src/core/app/state/step2_action.rs` | `Step2Action::OpenModDownloadSourceEditor { … }` | 23–28 | Add a `destination` field (a new small enum `ModSourceEditDestination { GlobalDefault, ThisModlist }`, defined in this file or `state_step2.rs`). **F6: derive `Debug, Clone, Copy, PartialEq, Eq, Default` (with `#[default] GlobalDefault`)** — `Eq` is REQUIRED because `Step2Action` derives `Eq` (`step2_action.rs:4`) and the field rides on `Step2Action::OpenModDownloadSourceEditor`; the enum is a plain field on `Step2State` (not the `Flag` parameter). | The enum's `Default` / the dropdown's default is `GlobalDefault` = today's path; legacy callers and the list-grid "Add Mod" path pass `GlobalDefault`. |
| `src/core/app/state/state_step2.rs` | `Step2State` editor fields | 62–68 + Default 142–148 | Add `mod_download_source_editor_destination: ModSourceEditDestination` + its `Default` init (`GlobalDefault`). **F6: this is a plain CONCRETE field — NOT a `Flag` type parameter** (`Step2State<Flag = bool>` is generic over `Flag`, but the destination is a concrete enum, so it is declared as `mod_download_source_editor_destination: ModSourceEditDestination`, not `: Flag`). `ModSourceEditDestination` derives `Debug, Clone, Copy, PartialEq, Eq, Default` so it composes with both `Step2State`'s `#[derive(Debug, Clone, PartialEq)]` and `Step2Action`'s `#[derive(Debug, Clone, PartialEq, Eq)]` (`Eq` required by the latter, `step2_action.rs:4`). No serde concern (`Step2State` is not serde-serialized). | New field defaults to the global-default destination; the struct's other fields and behavior are unchanged. The legacy binary never sets it to `ThisModlist`. |
| `src/core/app/app_step2_router.rs` | `OpenModDownloadSourceEditor` arm | 92–97 | Thread the new `destination` through to `open_mod_download_source_editor`. | Default arm value is `GlobalDefault`. |
| `src/core/app/app_step2_router.rs` | `open_mod_download_source_editor(...)` | 256–284 | Accept `destination`; store it in `state.step2.mod_download_source_editor_destination`; pass the corresponding target path **and the matching seed-scope (F2)** to `load_user_mod_download_source_block`: `GlobalDefault` ⇒ global path + two-tier seed (§5: true global value, not the modlist pin); `ThisModlist` ⇒ ambient per-modlist path + full 3-tier seed. | `GlobalDefault` with the ambient unset ⇒ global path + a two-tier seed that equals today's 3-tier seed (no third pass exists) ⇒ today's seed exactly. |
| `src/core/app/app_step2_router.rs` | `save_mod_download_source_editor(...)` | 286–312 | Read `state.step2.mod_download_source_editor_destination`; map to a target path (`GlobalDefault` → `mod_downloads_user_path()`; `ThisModlist` → ambient per-modlist path, falling back to global if ambient is unset); pass it to `save_user_mod_download_source_block`. | With ambient unset OR destination `GlobalDefault`, the write targets the global file — today's behavior. |
| `src/ui/step2/update_check/update_check_popup_lists_step2.rs` | `render_source_choice_row` "Edit Source" button | 98–118 | Replace the single "Edit Source" button with a small destination dropdown (Poppins-reskinned `egui::ComboBox` or a two-item `redesign_btn` popup) whose selection sets the `destination` carried on the emitted `Step2Action::OpenModDownloadSourceEditor`. | The default selection is "My default" (`GlobalDefault`), preserving the current single-click behavior; the second option is "For this modlist". |
| `src/ui/step2/update_check/update_check_popup_lists_step2.rs` | `render_list_grid` "Edit Source"/"Add Mod" button | 271–291 | Same destination-carry on the action emitted here. **"Add Mod" stays global-only** (a new mod source has no modlist scoping decision in PR-1) — emit `GlobalDefault`. | Default/Add-Mod path is today's behavior. |
| `src/core/app/modlist_share.rs` | `export_modlist_share_code(state)` | 17–62 (esp. 27–30, 35) | **Gate on an active modlist (F4/§6):** the gate keys on the **ambient** (`active_modlist_downloads_path().is_some()`), NOT on the callsite — `content_install_row_step5.rs:236` is a *shared* export button (the `BIO_legacy` binary reaches it ambient-unset; the redesign workspace flow reaches the same line ambient-set). When the ambient is **unset** (the `BIO_legacy` binary), keep today's verbatim copy of `mod_downloads_user_path()` (27–30) **byte-for-byte** — no resolved-set bake. When an ambient **is set**, replace that copy with a **resolved-set serializer**: for each mod in this modlist (`state.step2.bgee_mods` ∪ `bg2ee_mods`), resolve via the 3-tier loader + `selected_source_ids` (reusing `resolve_mod_config_source` / `installed_source_id_for_mod` at 571–607); **skip a mod with no resolvable source (F5)** — no panic, no malformed block; if the result is empty, emit an empty/absent `mod_downloads_user_toml` (importer filters empty text, modlist_share.rs:134). Emit each resolved source via the **new dedicated `serialize_resolved_source_block` serializer (F3)** — NOT `source_to_editor_block` (it duplicate-keys multi-`exact_github` and hardcodes `type`). Scope `installed_refs` content (31–34) to this modlist's selections rather than the global file verbatim. | Gated on `active_modlist_downloads_path().is_some()`: with the ambient unset the function is byte-identical to today (legacy §8/§10.6). With an ambient set the resolver sees the per-modlist overlay and the bake is the §6 resolved-set payload. |
| `src/core/app/modlist_share.rs` | **new** `fn serialize_resolved_source_block(source: &ModDownloadSource) -> String` *(F3)* + `fn serialize_resolved_mod(tp2, label, source) -> String` | new private items in this file | A correct round-trippable serializer (NOT the editor's `source_to_editor_block`): emit `[[mods]]` header (reuse the `template_mod_header` shape), one `[[mods.sources]]` block per resolved source; `exact_github` as a **TOML array** `exact_github = ["a/b@1", "c/d@2"]` (never repeated scalar lines); emit `repo = "<github>"` when `source.github.is_some()` (GitHub), else emit the `url`/`type` of the catalog source (`type = "url"`/`"page"` + `url`) so non-GitHub sources (e.g. Tenya/`type = "url"`, Verr'Sza/`type = "page"`) round-trip; intentionally **drop `config_files`** (their contents ride in the separate `mod_configs` payload — verified: the exporter reads `config_files` only to ship file bytes at modlist_share.rs:543, and the importer restores them by `relative_path` via `restore_pending_mod_configs_for_mod`, never re-reading the baked `config_files` list) and **`source_default`** (moot for a single-source-per-mod export — each exported mod resolves to exactly one source; the recipient's selected variant is driven by the `installed_refs`/`selected_source_ids` payload, not the baked block's `default`; `apply_source_overlay` only ever SETS default, never clears, so omitting it changes nothing). Validate by `toml::from_str::<ModDownloadsFile>` round-trip before baking. | Net-new private helper in an already-carve-out file; emits only into the **new** resolved-set bake which is itself gated on an active modlist — inert on the legacy/unset-ambient path. |
| `src/core/app/modlist_share.rs` | `import_modlist_share_code(state, code)` | 117–155 (esp. 130–137) | Redirect the pin write (136) from `mod_downloads_user_path()` to the **importing modlist's per-modlist path** — resolved from the **ambient** (`active_modlist_downloads_path()`), which the orchestrator sets to the new modlist's id **before** the import runs (F1: for paste-and-install this is the new seam **2d** in `finish_pipeline_arm_after_destination_prep`; for FORK it is `fork_pipeline_arm.rs:120`, already correct). Falls back to `mod_downloads_user_path()` when the ambient is unset. Leave the global file untouched. The `installed_refs` write (144–147) stays on its current path (see J3). | When ambient unset (legacy import via `BIO_legacy` Step-1), the write falls back to the global path = today's behavior. **F1: the ambient is now guaranteed set before this runs for paste-and-install (seam 2d), so the per-modlist redirect actually fires for the plain paste flow — it previously fell back to global because the id was minted after the import.** |

> **No new serializer *struct*; a small dedicated serializer *function* (F3).** `ModDownloadsFile` stays Deserialize-only — we do NOT add a `Serialize` derive. But the export resolved-set TOML must **not** reuse the editor's `source_to_editor_block`: that helper duplicate-keys multi-element `exact_github` (emits repeated scalar lines, `mod_downloads.rs:785-790` → a toml 0.8 duplicate-key parse error that aborts the export), hardcodes `type = "github"`, and emits `repo` only for GitHub sources. Instead, PMD.T6 adds a small hand-emitted **`serialize_resolved_source_block`** (emitting `exact_github` as a TOML array and the correct `repo`/`url`/`type` per source kind), validated by round-tripping through `toml::from_str::<ModDownloadsFile>` exactly like the writer does. `template_mod_header` + `normalize_source_block_indent` are still reused for the `[[mods]]` header + indentation. **Emit only the pin selectors (`tag`/`commit`/`branch`/`exact_github`) and source identity (`id`/`label`/`repo` or `url`/`type`): other non-selector fields (`channel`, `asset`, `subdir_require`, `tp2_rename`, `pkg_*`, `aliases`) inherit from the app-default tier via the field-wise overlay on import, so omitting them is a no-op — same inheritance logic as the dropped `source_default`.** The per-modlist **import** write is a raw verbatim whole-file `write_text_file` to the per-modlist path (T7 redirects `modlist_share.rs:136`) — correct because the baked text is a complete `ModDownloadsFile` (round-trip-validated at export); it does NOT use `save_user_mod_download_source_block` (that helper edits a single block and would be wrong for a whole file). Only the **export** bake needs the new serializer.

---

## Implementation tasks

Stable task IDs `PMD.T1`–`PMD.T8`. Dependency order is T1 → T2 → (T3 ‖ T4) → T5 → T6 → T7 → T8.

### PMD.T1 — Ambient active-modlist setter + third loader pass (reads)

- **What:** Add to `mod_downloads.rs` a process-global `static` holding `Option<PathBuf>`, a `pub(crate) fn set_active_modlist_downloads_path(Option<PathBuf>)`, and a private accessor. Extend `load_mod_download_sources()` with a third overlay pass (default → global-user → per-modlist) gated on `Some(path)` AND `path.exists()`, applied field-wise via the existing `apply_source_overlay` machinery, with `clear_other_source_defaults` honored for an explicit per-modlist default exactly as the global-user pass does.
- **Where:** `src/core/app/mod_downloads.rs` (carve-out #16: module top + `load_mod_download_sources` ~283–331).
- **Acceptance:**
  - With the ambient `None`, `load_mod_download_sources()` returns a `BTreeMap` byte-identical to today (unit test: snapshot the sources Vec with ambient unset, assert equality before/after the pass exists).
  - With the ambient `Some(temp per-modlist file)` setting `tag = "v16"` on one mod, the resolved source for that tp2 has `tag = Some("v16")` and inherits URL/repo/label from the lower tiers (field-wise merge); all other mods resolve identically to the two-tier result.
  - The static is a sound process-global (e.g. `Mutex<Option<PathBuf>>` or `RwLock`); no `unsafe`.
- **SPEC refs:** focused §3, §4, §9.1; SPEC.md §1 carve-out #16 (to be formalized).

### PMD.T2 — Orchestrator sets/clears the ambient at the three seams

- **What:** A net-new redesign-owned helper that the orchestrator calls to set the ambient to `modlist_data_dir(id).join("mod_downloads_user.toml")` when a modlist is active and to `None` when none is, wired at: **(a) workspace open / active** (`page_router::render` — when `rendered_nav` is `Workspace { Some(id) }` set it; when it is `Home`/`Install`/`Create`/`Settings` clear it), **(b) install start** (`start_hooks::write_install_start_artifacts(modlist_id, …)` — set it to the installing modlist's path before `pack_meta`/export runs), **(c) FORK import** (`fork_pipeline_arm.rs::mint_and_arm` already sets `active_install_modlist_id` at line 120 before the Downloading stage runs the import — set the ambient there too, to the freshly-minted id's path), **(d) PASTE-AND-INSTALL import (F1 — the critical fix)**: in the redesign-owned `stage_downloading.rs::finish_pipeline_arm_after_destination_prep`, mint the modlist id and set the ambient to its per-modlist path **before** the call to `prepare_install_dirs_and_maybe_import` (`stage_downloading.rs:699`), because that call runs `import_modlist_share_code` and the id was previously not minted until `register_and_write_install_start_artifacts` ran *after* it (`stage_downloading.rs:726` → `install_modlist_registration.rs:128`). The set/clear discipline is the required correctness invariant (focused §4): a stale ambient re-introduces the bleed.
- **F1 mechanism — chosen: option (a), early-mint in the redesign-owned arm; option (b) rejected.** Two options were evaluated for getting the importing modlist's path established before the paste-and-install import write:
  - **(a) — CHOSEN.** Mint the id + set the ambient in `finish_pipeline_arm_after_destination_prep` (redesign-owned) before `prepare_install_dirs_and_maybe_import`. **Verified safe:** the existing `install_start_modlist_id` (`install_modlist_registration.rs:138-162`) already reuses an entry by destination (`existing_entry_id_for_destination`, :154) and by reinstall id, so an early registration is **idempotent** — the later `register_and_write_install_start_artifacts` reuses the same id rather than minting a second one. Nothing depends on the entry being absent until install completes (the entry is created `InProgress` and only flips to `Installed` on clean exit). Extract the id-derivation (`install_start_modlist_id` + `register_new_install_start_modlist`'s persist) so it can run early; the artifact-write + `active_install_modlist_id = Some(id)` still happen in `register_and_write_install_start_artifacts`, now reusing the early id. This keeps **T7's import redirect uniformly ambient-based** across every import path (legacy, fork, paste).
  - **(b) — REJECTED.** Add an explicit `target_path: Option<&Path>` param to `import_modlist_share_code` (T3-style) and pass the new modlist's path. Rejected because it introduces a **second** import-routing mechanism (explicit param) alongside the ambient that T7 already uses for the redirect — two ways to do the same thing on the same BIO function, more carve-out surface, and the orchestrator would still have to derive the id early anyway (to know the path to pass). Option (a) reuses the existing idempotent registration and needs no new BIO param.
  - **(F1 round-2) Roll back the early-mint on import failure.** Because the mint now runs *before* `prepare_install_dirs_and_maybe_import`, a failed import (empty code `auto_build_driver.rs:35-37`; `import_modlist_share_code` errors — EET WLB path `modlist_share.rs:357`, missing log folder `:419`/`:433`) would otherwise leave an **orphan `InProgress` registry entry** that today (mint-after-import) would not exist. The extracted early-mint helper returns whether it **freshly minted** (vs reused an entry by destination-dedup); on the `Err` arm of the import (`stage_downloading.rs:730-737`), if it freshly minted this call, remove the entry from the registry + persist (roll back); reused entries are left intact. (Acceptable alternative: treat the orphan as the same class as the existing cancel/nav-away orphan, `page_router.rs:230-238`, and let the existing cleanup GC it — but rollback is preferred, to avoid introducing a new orphan source.)
- **Where (new):** `src/install_runtime/active_modlist_source_path.rs`. **Where (modified, redesign-owned):** `src/ui/orchestrator/page_router.rs::render` (set/clear on nav), `src/install_runtime/start_hooks.rs` (set at install start), `src/install_runtime/fork_pipeline_arm.rs::mint_and_arm` (set ambient at the existing id-set, ~line 120), `src/ui/install/stage_downloading.rs::finish_pipeline_arm_after_destination_prep` (early-mint + set ambient before `prepare_install_dirs_and_maybe_import`), and `src/install_runtime/install_modlist_registration.rs` (split the id-derivation out so it can run early, idempotently — this file is redesign-owned). No BIO source touched beyond the `pub(crate)` setter from T1.
- **Acceptance:**
  - Navigating Home → Workspace(A) → Home leaves the ambient `None` after returning Home (unit/integration test on the helper's set/clear logic; the page_router wiring is exercised by a render-path test or a focused logic test of the nav→ambient mapping function).
  - Switching Workspace(A) → Workspace(B) refreshes the ambient to B's path (no stale A).
  - **F1:** the paste-and-install arm sets the ambient to the new modlist's path **before** `import_modlist_share_code` runs (assert in a logic test of the extracted early-mint helper: the id is minted and the ambient is non-`None` at the point the import would be invoked; and that the later registration reuses — does not re-mint — that id). Pairs with PMD.T7's paste-and-install import test.
  - `BIO_legacy` never links this helper (it lives in `install_runtime`, orchestrator-only); the legacy binary's ambient is always `None`. Verified by `cargo build --bin BIO_legacy --release` succeeding without the orchestrator modules.
- **SPEC refs:** focused §4, §8; §9.3; §10.5 (F1).

### PMD.T3 — Target-path capability on the source-block load/save (writes plumbing)

- **What:** Add a target-path parameter to `save_user_mod_download_source_block` and `load_user_mod_download_source_block` so a write/seed can target the per-modlist file. Global path remains the default. The text-block replace/append/validate/write logic is otherwise untouched; `ensure_mod_downloads_files()` still seeds the global files. **F2 (§5): the editor seed becomes tier-matched** — add a seed-scope selector to `load_user_mod_download_source_block`; `ThisModlist` seeds from the full 3-tier `load_mod_download_sources().resolve_source(...)` (today's behavior), `GlobalDefault` seeds from a **two-tier** resolution (the true global / app-default value, WITHOUT the per-modlist overlay) so opening + saving "My default" never promotes a modlist pin into global. **Mechanism:** factor the existing default→global-user merge body (`load_mod_download_sources`, ~290–323) into a private `load_two_tier_sources()` helper; the no-arg `load_mod_download_sources()` calls it then runs the third pass (T1); the `GlobalDefault` seed calls `load_two_tier_sources().resolve_source(...)`. Only the existing-user-block lookup honors the chosen destination path.
- **Where:** `src/core/app/mod_downloads.rs` (carve-out #16: 209–230, 232–281; plus the `load_two_tier_sources()` extraction at 283–331 and the new global-seed path).
- **Acceptance:**
  - Calling the writer with the global path produces a file byte-identical to today's writer for the same input (regression unit test).
  - Calling the writer with a temp per-modlist path writes only that file; the global file (a separate temp fixture) is byte-unchanged.
  - The "For this modlist" seed with no per-modlist block yet pre-fills the 3-tier resolved (effective) source, same shape as a fresh global edit.
  - **F2:** with an ambient set + a per-modlist pin on a mod, the `GlobalDefault` seed pre-fills the **two-tier (global)** value, NOT the modlist's pin; the `ThisModlist` seed pre-fills the 3-tier value (the pin). With the ambient unset, both seeds equal today's value (the two-tier and 3-tier resolutions coincide).
- **SPEC refs:** focused §5, §9.2; §10.3 (F2 — "My default" never touches a per-modlist file and shows the global value).

### PMD.T4 — Destination choice on the action + editor state (writes wiring)

- **What:** Add the `ModSourceEditDestination` enum (`GlobalDefault` default, `ThisModlist`), carry it on `Step2Action::OpenModDownloadSourceEditor`, store it on `Step2State`, and read it at save to pick the target path (`GlobalDefault` → global; `ThisModlist` → ambient per-modlist path, falling back to global when ambient is unset). Wire `open_mod_download_source_editor` (passing the matching seed-scope per T3/F2) and `save_mod_download_source_editor` accordingly.
- **F6 — field shape & derives.** `ModSourceEditDestination` derives `Debug, Clone, Copy, PartialEq, Eq, Default` (with `#[default] GlobalDefault`) — `Eq` is required because the field rides on `Step2Action`, which derives `Eq`. On `Step2State<Flag = bool>` the field is a **plain concrete field** `mod_download_source_editor_destination: ModSourceEditDestination` — NOT a `Flag`-typed field (the generic `Flag` parameter is for the bool-like UI flags only). This preserves `Step2State`'s `#[derive(Debug, Clone, PartialEq)]`. No serde concern (`Step2State` is not serialized).
- **Where:** `src/core/app/state/step2_action.rs` (23–28), `src/core/app/state/state_step2.rs` (62–68, 142–148), `src/core/app/app_step2_router.rs` (92–97, 256–284, 286–312). All carve-out #16.
- **Acceptance:**
  - `Step2Action` and `Step2State` still derive their existing traits; equality tests (if any) pass with the new field defaulting to `GlobalDefault`.
  - A save with `GlobalDefault` targets the global path; a save with `ThisModlist` (ambient set to a temp per-modlist path) targets that path; both verified by byte-diffing the two temp files.
  - **F2:** a save with `GlobalDefault` while a per-modlist pin exists leaves the per-modlist temp file **byte-unchanged** (the destination routing never writes the per-modlist file for "My default").
  - With `ThisModlist` but ambient unset, the save falls back to the global path (legacy safety).
- **SPEC refs:** focused §5, §9.4; §10.2/§10.3.

### PMD.T5 — Set-Source destination dropdown (write UI)

- **What:** Replace the per-row "Edit Source" button with a destination dropdown offering "My default" and "For this modlist"; the selection rides the `destination` field on the emitted `OpenModDownloadSourceEditor` action. Applies to `render_source_choice_row` (the Source-Choices grid) and to the `render_list_grid` "Edit Source" path; the "Add Mod" path stays `GlobalDefault`. Reskin with the existing redesign tokens / `redesign_btn` already used in this file (carve-out #9 chrome precedent).
- **Where:** `src/ui/step2/update_check/update_check_popup_lists_step2.rs` (98–118, 271–291). Carve-out #16 (UI structural reskin scoped to the destination affordance).
- **Acceptance:**
  - The Updates popup shows a "My default" / "For this modlist" choice on each editable source row; default selection is "My default" (today's single-click behavior preserved).
  - Choosing "For this modlist" then editing+saving writes the active modlist's per-modlist file; "My default" writes the global file. Manual-test verifiable (see Test plan).
  - Grid column count / layout stays coherent (the dropdown replaces the single button cell; if a column is added, `num_columns` is bumped consistently — verify the grid still renders without overflow).
- **SPEC refs:** focused §5, §9.4.

### PMD.T6 — Export bakes the full resolved set (modlist-scoped)

- **What:** Make the export's `source_overrides.mod_downloads_user_toml` modlist-scoped, **gated on an active modlist (F4/§6)**:
  - **Ambient unset (the `BIO_legacy` binary — it reaches the shared `content_install_row_step5.rs:236` export button with the ambient never set):** preserve today's behavior **byte-for-byte** — the verbatim copy of `mod_downloads_user_path()` via `read_optional_file_text(...)` stands. No resolved-set bake. §10.6 "byte-identical" applies here. (The gate is on the ambient, so the same `:236` line in the redesign workspace flow takes the ambient-**set** branch below.)
  - **Ambient set (redesign workspace Share / install-start):** replace the verbatim copy with a **resolved-set serializer**. Resolve every mod in the modlist (`state.step2.bgee_mods` ∪ `bg2ee_mods`) through the 3-tier loader + `selected_source_ids` (reuse `resolve_mod_config_source` / `installed_source_id_for_mod`, modlist_share.rs:571-607). **F5:** skip any mod with no resolvable source (the `let Some(source) = … else { continue }` precedent at :532-536) — no panic, no malformed block; if no mod resolves, emit an empty/absent `mod_downloads_user_toml` (the importer filters empty text at :134, so an empty `source_overrides` is allowed — the workspace export path always has scanned mods, so this is the sparse/edge guard, not the common case).
  - **F3 — correct serializer.** Emit each resolved source via the **new dedicated `serialize_resolved_source_block`** (PMD.T6 helper), NOT the editor's `source_to_editor_block`. The editor helper duplicate-keys multi-element `exact_github` (repeated scalar lines, mod_downloads.rs:785-790 → toml 0.8 duplicate-key parse error that would abort the export), hardcodes `type = "github"`, and emits `repo` only for GitHub sources. The new serializer: `exact_github` as a **TOML array**; `repo = "<github>"` for GitHub sources (`source.github.is_some()`), else the catalog `type`/`url` for non-GitHub sources (`type = "url"`/`"page"` + `url` — e.g. Tenya/Verr'Sza in `default_mod_downloads.toml`); **drops `config_files`** (contents ride in the separate `mod_configs` payload — verified at modlist_share.rs:543 export + `restore_pending_mod_configs_for_mod` import, which keys on `relative_path`, never the baked list) and **`source_default`** (moot single-source-per-mod — the recipient's variant is driven by the `installed_refs`/`selected_source_ids` payload; `apply_source_overlay` only ever sets default, never clears, so omitting it is a no-op). Round-trip-validate the assembled TOML through `toml::from_str::<ModDownloadsFile>` before baking.
  - Scope the `installed_refs` payload content to this modlist's selections.
- **Where:** `src/core/app/modlist_share.rs::export_modlist_share_code` (17–62, esp. 27–30 + 35) + the new `serialize_resolved_source_block` private helper. Carve-out #16. The active-modlist gate reads `crate::app::mod_downloads::active_modlist_downloads_path().is_some()` (the T1 accessor).
- **Acceptance:**
  - Pin CDTweaks v16 "For this modlist" on Modlist A, Master/v19 "For this modlist" on Modlist B (any write order). With ambient(A) set, `export_modlist_share_code` yields a code whose `source_overrides` resolves CDTweaks to **v16**; with ambient(B), **v19**. (Focused §10.1.)
  - The exported `source_overrides` is **not** a verbatim copy of the global file (a global file with arbitrary sentinel content does not appear in A's exported code).
  - The resolved TOML round-trips through `toml::from_str::<ModDownloadsFile>` (validity gate).
  - **F3:** a mod whose resolved source has a multi-element `exact_github` exports a single `exact_github = [...]` array line (no duplicate-key abort); a non-GitHub source (`type = "url"`/`"page"`) exports with its `url` and no `repo`, and re-importing it yields `github = None` (download path stays URL-based, `source_is_auto_resolvable` via url, mod_downloads.rs:879-884).
  - **F4:** with the ambient **unset**, the export is **byte-identical** to today's verbatim-copy export for the same WizardState (legacy branch — assert the exported `source_overrides` equals the verbatim global-file text). With the ambient **set**, it is the resolved-set payload (assert it is NOT the verbatim global text).
  - **F5:** an export where a scanned mod has no resolvable source skips that mod (no panic, valid TOML); an export where no mod resolves yields an empty/absent `source_overrides` (still a valid share code).
- **SPEC refs:** focused §6, §8, §9.5, §10.1, §10.6.

### PMD.T7 — Import lands pins in the modlist's file

- **What:** Redirect the imported `mod_downloads_user_toml` pin write (modlist_share.rs:136) from the global path to the importing modlist's per-modlist path, resolved from the **ambient** (`active_modlist_downloads_path()`), set to the new modlist's id by the orchestrator **before** the import runs. Leave the global file byte-unchanged. Installed-refs write stays on its current path (J3).
- **F1 — the ambient must be set before the import for EVERY import path.** Previously the plan assumed the orchestrator set the ambient at import; for **paste-and-install** it did not, because the id was minted *after* the import (`stage_downloading.rs:726` runs after the import at `:699`). PMD.T2 seam **2d** now mints the id + sets the ambient before `prepare_install_dirs_and_maybe_import`, so the redirect actually fires for the plain paste flow. The FORK path was already correct (`fork_pipeline_arm.rs:120`); legacy `BIO_legacy` Step-1 paste stays intentionally global (ambient never set).
- **Where:** `src/core/app/modlist_share.rs::import_modlist_share_code` (117–155, esp. 130–137). Carve-out #16. Plus the orchestrator import seams set the ambient (covered by T2c/T2d).
- **Acceptance:**
  - Importing a code into a new modlist writes `modlists/<new-id>/mod_downloads_user.toml`; the global file is byte-unchanged (focused §10.5).
  - The user's other modlists' per-modlist files are byte-unchanged.
  - **F1 — paste-and-install:** with the ambient set to a temp new-modlist path (mirroring seam 2d) BEFORE `import_modlist_share_code`, importing a code with non-default pins writes the **per-modlist** temp file and leaves the temp global file **byte-unchanged**. (This is the test that would have caught the inert-import regression — it must exercise the ambient-set-before-import ordering, i.e. drive through or simulate seam 2d, not call the import with the ambient still `None`.)
  - With ambient unset (legacy `BIO_legacy` Step-1 import), the write falls back to the global path = today's behavior (focused §10.6).
- **SPEC refs:** focused §7, §9.6, §10.5, §10.6.

### PMD.T8 — Doc-sync: formalize carve-out #16

- **What:** Add carve-out #16 to `SPEC.md §1` in the directive's required per-file/line-cited form (matching the #8–#15 style); update HANDOFF; add the §13.12b cross-reference reconciling the focused-spec decisions against the older "no new carve-out" / "composed unchanged" T14 text. (Detailed in Doc-sync owed.)
- **Where:** `infinity_orchestrator/SPEC.md` §1 + §13.12b cross-ref, `infinity_orchestrator/HANDOFF.md`. Docs only — not code.
- **Acceptance:** `SPEC.md §1` enumerates #16 with the exact files/functions/lines this plan touches; HANDOFF's "Doc-sync owed" reflects #16; §13.12b notes the supersession. (This is a docs PR / docs commit, separate from the code commit(s) per the per-run-branch workflow.)
- **SPEC refs:** focused §9 (formalization mandate), spec-authority amend path.

---

## Verification

Run after each implementer run and at the umbrella close:

1. `cargo build --bin BIO --release` — final no-op rebuild proof (`Finished`, no `Compiling bio`).
2. `cargo test --lib` — record the count; behavior-neutral surfaces keep it; new tests add to it. `grep "test result:"` (not `^Finished`).
3. `cargo build --bin BIO_legacy --release` — the legacy binary still compiles and links (legacy-preservation gate).
4. **BIO-source-guard** on the staged set — the carve-out #16 files ARE protected-BIO paths, so the guard WILL flag them; that flag is *expected and authorized for exactly these files under #16*. Reconcile the staged set against this plan's carve-out #16 table — any BIO file NOT in that table is an ABORT-and-investigate.
5. **Legacy-preservation gate (verification item):** with the ambient unset, prove (a) `load_mod_download_sources()` returns the same sources as before (snapshot test), (b) the source editor writes the global file byte-identically, (c) **export is byte-identical to today's verbatim-copy path (F4 — the resolved-set bake is gated on an active modlist; with none, the verbatim copy stands)** and import writes the global file as today. A `BIO_legacy` smoke check (open Step-2 update flow, edit a source, confirm only the global file changes; Export Modlist and confirm `source_overrides` is the verbatim global file).
6. **DATA-LOSS sentinel (directive-grade):** snapshot `%APPDATA%\bio\modlists.json` + every seed `%APPDATA%\bio\modlists\<id>\workspace.json` + `%APPDATA%\bio\mod_downloads_user.toml` (global) before and after `cargo test --lib`; all byte-unchanged. No unit test may bind the real config dir — see Test plan.

## Test plan

> **DATA-LOSS invariant (hard):** no unit test may bind the real config dir (`%APPDATA%\bio\`). Any test that constructs `OrchestratorApp` or any `RegistryStore::new_default()` / `WorkspaceStore` repoints stores to a unique `std::env::temp_dir()` path (the `store.rs` / `dev_seed.rs` `temp_path()` precedent). Tests exercising the per-modlist file (under the config dir) MUST use temp dirs and set the ambient explicitly to a temp path — never let it resolve through `modlist_data_dir` into the real config dir.

> **AMBIENT TEST-ISOLATION invariant (hard — process-global hazard).** The ambient (`set_active_modlist_downloads_path`) is a **process-global** (`Mutex`/`RwLock<Option<PathBuf>>`). `cargo test --lib` runs tests **in-process**, so a test that sets the ambient and returns without clearing it **leaks** into every later test — and a test that *panics* while the ambient is set leaves it set, silently corrupting later loader/export tests that assume the two-tier baseline. **Mandate:** every ambient-touching test sets the ambient ONLY through an **RAII drop-guard** that restores it to the prior value (capture-on-`new`, restore-on-`Drop`) — never a bare `set_active_modlist_downloads_path(Some(...))` without teardown. The guard restores even on panic/early-return because `Drop` runs during unwind. Provide one test-only guard (e.g. a `struct AmbientGuard(Option<PathBuf>)` in the `mod_downloads` test module, or a shared `#[cfg(test)]` helper) and use it in **every** test below that touches the ambient: T1 third-pass tests, T2 seam tests, T4 `ThisModlist` routing, T6 export tests, T7 import tests. A test that needs the ambient unset asserts/forces `None` at start via the guard. **Mandatory shared test-lock (not optional).** Because `cargo test` is multi-threaded AND the loader/export the ambient feeds is read by ~20 pre-existing test files (e.g. `modlist_share.rs` export tests like `export_share_code_bakes_current_modlist_provenance`), the drop-guard alone is insufficient: a concurrent ambient-setting test can transiently flip the process-global mid-read of an unrelated test and leak a per-modlist overlay into its baseline. Every ambient-touching test — and any test that calls the loader/export while ambient tests exist — MUST hold a shared `static` `Mutex` test-lock for its full body so it runs critical-section-exclusive. The drop-guard (prior-value restore on panic/early-return) and the lock (mutual exclusion) are BOTH required.

**Loader (PMD.T1):**
- `ambient_unset_loader_matches_two_tier` — write temp default + temp global-user files, point the BIO path helpers (or drive via a temp-root injection) and assert the sources Vec equals the two-tier baseline; ambient `None`.
- `ambient_set_third_pass_overrides_field_wise` — temp per-modlist file sets only `tag`; assert the resolved source has the new tag and inherits URL/repo/label from lower tiers.
- `ambient_set_but_file_absent_is_inert` — ambient `Some(nonexistent)`; resolution equals the two-tier result.
- `explicit_per_modlist_default_clears_other_defaults` — a per-modlist `source_default` flips the default for that tp2, mirroring the global-user pass.

**Ambient set/clear (PMD.T2):** *(all use the ambient drop-guard)*
- `nav_to_workspace_sets_clear_to_home_clears` — test the pure nav→Option<PathBuf> mapping function (Home/Install/Create/Settings → None; Workspace{Some(id)} → Some(modlist_data_dir(id)/file)).
- `workspace_switch_refreshes_ambient` — A→B yields B's path, never stale A.
- `paste_install_early_mint_sets_ambient_before_import` *(F1)* — test the extracted early-mint helper from seam 2d: it mints an id, sets the ambient non-`None`, and the later registration reuses (does not re-mint) that id (idempotent via destination-dedup).
- `import_fails_after_early_mint_rolls_back` *(F1 round-2)* — a freshly-minted entry is removed from the registry on the import `Err` arm; a reused (destination-deduped) entry is left intact.

**Writer target-path + global seed (PMD.T3):**
- `writer_global_path_matches_today` — same input through the writer with the global path equals today's output (regression).
- `writer_per_modlist_path_isolates` — write to temp per-modlist path; the temp global file is byte-unchanged.
- `two_tier_seed_equals_three_tier_when_ambient_unset` — with the ambient `None`, `load_two_tier_sources()` equals `load_mod_download_sources()` (the extraction is behavior-neutral).
- `global_seed_ignores_per_modlist_pin` *(F2)* — ambient set + a per-modlist pin (`tag = "v16"`); the `GlobalDefault` seed pre-fills the two-tier (global) value, NOT v16; the `ThisModlist` seed pre-fills v16.

**Destination wiring (PMD.T4):**
- `editor_destination_defaults_global` — `Step2State::default()` has `GlobalDefault`; equality unchanged; `ModSourceEditDestination` derives `Debug, Clone, Copy, PartialEq, Eq, Default` *(F6)*.
- `save_routes_by_destination` — `GlobalDefault` → global temp file; `ThisModlist` (ambient temp) → per-modlist temp file; assert byte-diffs.
- `global_default_save_leaves_per_modlist_untouched` *(F2)* — with a per-modlist pin present, a `GlobalDefault` save updates only the global temp file; the per-modlist temp file is byte-unchanged.
- `this_modlist_with_unset_ambient_falls_back_global` — legacy safety.

**Export (PMD.T6):**
- `export_bakes_resolved_set_not_verbatim_global` — ambient(A) set to a temp per-modlist file pinning a mod to v16, a sentinel string in the global file; assert the exported `source_overrides` contains v16 and NOT the sentinel.
- `export_modlist_a_v16_modlist_b_v19` — the focused §10.1 cross-modlist acceptance (two temp per-modlist files, two exports, order-independent).
- `export_ambient_unset_is_byte_identical_verbatim` *(F4)* — with the ambient unset, the exported `source_overrides.mod_downloads_user_toml` equals the verbatim global-file text (today's legacy behavior, byte-for-byte).
- `export_serializer_emits_exact_github_as_array` *(F3)* — a resolved source with a multi-element `exact_github` serializes to a single `exact_github = [...]` array line and round-trips through `toml::from_str::<ModDownloadsFile>` (no duplicate-key abort).
- `export_serializer_non_github_source_url_no_repo` *(F3)* — a `type = "url"`/`"page"` (weaselmods / direct-URL) resolved source serializes with its `url` and no `repo`; re-parsing yields `github = None`.
- `export_skips_unresolvable_mod` *(F5)* — a scanned mod with no resolvable source is skipped (valid TOML, other mods present).
- `export_empty_source_overrides_when_nothing_resolves` *(F5)* — no mod resolves ⇒ empty/absent `mod_downloads_user_toml`, still a valid share code.

**Import (PMD.T7):**
- `import_writes_per_modlist_not_global` — ambient set to temp new-modlist path; import a code with non-default pins; assert the per-modlist temp file gets them and the temp global file is byte-unchanged (focused §10.5).
- `paste_and_install_import_writes_per_modlist_global_unchanged` *(F1)* — exercise the seam-2d ordering (ambient set to a temp new-modlist path BEFORE `import_modlist_share_code`); assert the per-modlist temp file gets the pins and the temp global file is byte-unchanged. This is the regression guard for the inert-import bug.
- `import_ambient_unset_writes_global` — legacy fallback.

**Integration / sentinel:** snapshot the DATA-LOSS sentinel set before/after the whole `cargo test --lib`; assert byte-stable. No test keeps a `new_default()` store.

**Manual-test script (for the PR body):**
1. Open Workspace for Modlist A (e.g. an EET build). In Step-2 → Update Check, for CDTweaks click "Edit Source" → **For this modlist**, pin `tag = "v16"`, Save. Confirm the global `%APPDATA%\bio\mod_downloads_user.toml` is unchanged and `%APPDATA%\bio\modlists\<A>\mod_downloads_user.toml` now holds the v16 block.
2. Open Modlist B, pin CDTweaks Master/v19 "For this modlist". Confirm A's file still says v16.
3. From A's Step-5 (post-install) "Share import code", copy the code; decode/inspect (or import into a fresh modlist) — it must carry **v16**. Repeat from B — **v19**.
4. On a source row choose **My default**, edit + Save; confirm the global file updates and no per-modlist file is written. **(F2)** While Modlist A is active (it has a v16 pin), open **My default** for CDTweaks and confirm the editor pre-fills the **global** value (not v16); Save and confirm A's per-modlist file is untouched.
5. **(F1 — paste-and-install)** Paste a share code into Install (new modlist C) and run it; confirm **C's per-modlist file** (`modlists/<C>/mod_downloads_user.toml`) gets the pins and the **global** file is byte-unchanged. (Before this fix the pins clobbered the global file.)
6. **(F3)** Export a modlist containing at least one non-GitHub source (e.g. Verr'Sza/weaselmods `type = "page"` or Tenya `type = "url"`) and one source with a multi-element `exact_github`; confirm the export succeeds (no duplicate-key abort) and the decoded `source_overrides` shows `exact_github = [...]` as an array and the non-GitHub source's `url` with no `repo`.
7. Run `BIO_legacy`, open the update flow, edit a source — confirm only the global file changes (no per-modlist files created). **(F4)** From `BIO_legacy` Step-5, Export Modlist and confirm the `source_overrides` is the verbatim global file (today's behavior, byte-identical).

## Doc-sync owed

1. **`SPEC.md §1` — formalize carve-out #16** in the directive's per-file/line-cited form (match the #8–#15 entries' style). It must enumerate the six functional surfaces of focused §9 **plus the two review-round-1 sub-edits they require** — (a) the private `load_two_tier_sources()` extraction + the global-only editor seed (F2/§5: the "My default" pre-fill resolves WITHOUT the per-modlist overlay), and (b) the dedicated `serialize_resolved_source_block` export serializer (F3: correct `exact_github` array + non-GitHub `url`/`type`, replacing the lossy `source_to_editor_block` reuse) — and cite every file/function/line in this plan's carve-out #16 table. Note the active-modlist gate on the export bake (F4/§6). Confirm #16 is the next free number against live `SPEC.md §1` (current highest is **#15**, line 55) before writing.
2. **`SPEC.md §13.12b` cross-ref** — add a note that the per-modlist *download-source* slice (`mod_downloads_user.toml`) lands via **carve-out #16 + an ambient resolver**, per the 2026-06-08 locked sub-spec, *superseding* §13.12b's "No new carve-out required" line and the older `P8.T14.4` "composed unchanged" export design for this overlay. The compat-rules / installed-source-refs overlays remain under §13.12b / carve-out #5 as written.
3. **`HANDOFF.md`** — move the carve-out #16 / per-modlist-download-source work into the active/shipped record; note the existing #11–#14 doc-sync debt (HANDOFF line 36) is unaffected and that HANDOFF's claim "SPEC.md §1 enumerates through #9" is stale (it actually enumerates through #15) — correct it.
4. **`phase-08-popup-reskins-polish.md` (P8.T14)** — annotate that the mod-source slice of T14.3–T14.5 is delivered by this plan via the ambient/carve-out-#16 design (not the orchestrator-interception/composed-unchanged design previously written there); the compat-rules + installed-refs slices remain future T14 work.

---

## Run / PR slicing recommendation (for the orchestrator)

> **Recommendation — one implementer run, then a separate docs commit. NOT three runs.**

The user framed this as a single "PR-1 (correctness core)". The work is tightly coupled around one mechanism (the ambient + the loader seam) and is small in absolute size: ~3 BIO files for reads/writes (the round-1 F2 two-tier helper + F3 export serializer are additional functions *within* `mod_downloads.rs` / `modlist_share.rs`, not new files), ~3 for action/state/router, the same 2 for export/import, 1 net-new orchestrator file, plus a handful of redesign-owned F1 edits (`stage_downloading.rs`, `fork_pipeline_arm.rs`, `install_modlist_registration.rs` — all already redesign-owned), plus the UI dropdown. Slicing it would create artificial inter-run dependencies (writes can't be manually verified without reads; export can't be verified without writes; the ambient must exist before any of them is testable) and multiply the DATA-LOSS sentinel checks without reducing risk.

**Land as:**
- **Run 1 (code, one PR):** PMD.T1 → T7 as a single `plan-implementer` run on one branch. It is internally consistent and independently verifiable end-to-end via the manual-test script (the only complete way to confirm the named user-facing outcome — "export bakes the right version"). The carve-out #16 edits are all additive/inert on the unset-ambient path, so the legacy gate is satisfiable in the same run.
- **Docs commit (T8):** formalize carve-out #16 + HANDOFF + §13.12b/T14 cross-refs. Per the project's per-run-branch workflow this is its own `docs/…` branch/PR (pure-docs ⇒ skip the rebuild gate). Land it *after* the code PR merges so the line-citations match the merged code.

**If the orchestrator prefers a two-slice de-risking** (optional, not required): Slice A = T1+T2+T3+T4 (the ambient + reads + writes plumbing/wiring, no user-visible change yet — internally consistent, all tests green, global behavior unchanged); Slice B = T5+T6+T7 (the dropdown UI + export/import, which flips the user-facing behavior and closes the bleed). Slice A is safe to merge alone (plumbing present, defaults to global). This mirrors the §13.12b Part-A/Part-B precedent. **Default remains one run**; offer the two-slice only if the diff feels large at implementation time.

---

## SPEC CONFLICT

None. The review-round-1 fixes (F1–F6) stay inside the §5/§6 clarifications and carve-out #16's authorized six-surface envelope: the two-tier global seed (F2) and the active-modlist export gate (F4) are exactly what §5/§6 now mandate; the dedicated export serializer (F3) is a correctness sub-edit of the §9.5 export surface (it does not add behavior, it fixes a serialization defect); the F1 early-mint lives entirely in redesign-owned files.

## PLAN GAP

None blocking. Two judgment calls and one reconciliation are surfaced below for orchestrator/user review.

**F3 field-drop verification (grounded for the re-review).** Both intentionally-dropped fields were checked against the live import path: (1) `config_files` — the exporter reads them only to ship file bytes into the separate `mod_configs` payload (`modlist_share.rs:543`); on import, `restore_pending_mod_configs_for_mod` (`modlist_config_files.rs:82-124`) writes those files by `relative_path` keyed on tp2+source_id+aliases, never re-reading a baked `config_files` list — so dropping it from the source block changes nothing. (2) `source_default` — the recipient's installed variant is driven by the imported `installed_refs` → `selected_source_ids` (`import_modlist_share_code` sets `state.step2.selected_source_ids = load_installed_source_ids()` at `modlist_share.rs:151-152`), and the baked block is a field-wise overlay keyed by `source_id` on top of the full default catalog; `apply_source_overlay` (`mod_downloads.rs:1025`) only ever *sets* `source_default = true`, never clears, so omitting it is a no-op. Both claims are therefore verified, not assumed.

## Judgment calls (propose — do not silently incorporate)

- **J1 — Carve-out #16 vs the older T14 orchestrator-interception design.** The focused spec §9 *explicitly authorizes carve-out #16* and chose ambient-resolution + BIO edits over the `P8.T14.4` orchestrator-side interception. This plan follows the focused spec (the newer locked authority). The consequence is that BIO source IS edited (under #16) where T14.4 would have avoided it. This is intended by the sub-spec, not scope creep — but it changes the §13.12b "no new carve-out" posture for this overlay, hence the Doc-sync reconciliation. Flagging because it is a directive-surface change the user authorized in the sub-spec but which the older plan doc contradicts.
- **J2 — Ambient mechanism is a process-global.** The sub-spec (§4 decision note) chose ambient over threading an explicit modlist argument through every resolution call site, to keep the protected-source edit surface minimal. A process-global is the natural implementation but carries the stale-ambient re-bleed risk; the plan makes the set/clear discipline a tested invariant (PMD.T2). If the user/orchestrator prefers an explicit thread despite the larger edit surface, that is a sub-spec change, not a plan change. Following the sub-spec as written.
- **J3 — Installed-refs file stays global; only its export *content* is modlist-scoped.** The focused spec §3 data model names **only** `mod_downloads_user.toml` as the new per-modlist tier. §6 scopes the *content* of the export's installed-refs companion to this modlist's selections (already per-`WizardState`), and §7 is silent on installed-refs. So this plan does NOT create a per-modlist `installed_source_refs.toml` file and does NOT redirect the import's installed-refs write (modlist_share.rs:144–147) off the global path — that belongs to the broader §13.12b / T14 work, out of scope here. If the user intended import to also isolate installed-refs per-modlist in PR-1, that is a sub-spec clarification — surfacing rather than assuming.
