# Phase 6 — Create screen + Workspace shell (Steps 2-4)

**Status:** SHIPPED — full audit record: [`archive/phase-06-create-workspace-shell.md`](archive/phase-06-create-workspace-shell.md).

**Commit anchors:** Run 1 spine `150593f` (+ `4a7260b` M2 follow-up, `3d28acb` re-prep seed) → Run 1b `b3c4823` + `832845d` + `9dc85d5` (Show/Hide Details verb) → Run 1c `354920d` → Run 1d `a57ba1d` → Run 1e `17b1dcb` → Run 2 `6df87f9` (Step-4 C4 + header/rename + save-draft + game tabs, P6.T2b/T5/T6/T10) + `301bf72` test-clobber fix → Run 2b `4950b19` + `ee993a7` (weidu 3-hue) + doc-sync `0e099a4` + `9b5b9d5` Step-3-rebuild fix + `75d8d59` doc-sync → workspace alignment / uniform GameTab `ab4453b` + `99bcb45` retraction → P6.T2d Step-3 C4 `fad78c3` → Run 3 `8fa52f8` (Create stage_choose + Load Draft + routing, P6.T7/T9/T13/T14) + `430fb4f` / `32893b6` skill hardening → Run 4 `ed146dd` "Phase 6 Run 4 SHIPPED — fork sub-flow + dirty-bit + nav-away flush (P6.T8/T11/T15) — PHASE 6 COMPLETE". Subsequent post-phase fix-runs touching Phase-6 code: `8dfb905` Fix-Run 1, `41d70f7` Fix-Run 2 + `4ec1c09` escalation resolutions, `0b9d53d` Fix-Run 3 + `c9a8bab` doc-sync, `28d9975` Fix-Run 4 + `21db08d` doc-sync, `826d40d` DATA-LOSS arc CLOSED + sentinel hardening, `600dbb3` Fix-Run 5, `e305f92` Fix-Run 6 + `7086149` egui_kittest render gate + `9fbe020` / `4451e2b` doc-syncs, `4464634` Phase-6 verification plate cleared (#1/#3/#4 user-verified closed; FR2-4/#2 punted to Phase 8).

**What shipped:** the Create destination (choose mode + setup Box + selectable starting-point boxes + Load Draft dialog with Delete + fork-paste / fork-preview / fork-download sub-flow appending lineage via `pack_meta`) and the `WorkspaceView` shell hosting Steps 2-4. **Steps 2, 3, and 4 are orchestrator-side C4 renderers** — each rebuilds the wireframe's net-new chrome and reuses **only** BIO's heavy interaction sub-renderers read-only (Step 2: `list_pane_step2::render_list_pane` + `details_pane_step2::render_pane` + popups via P6.T2c `workspace_step2`; Step 3: `list_step3::render` via P6.T2d `workspace_step3`; Step 4: per P6.T2b's `workspace_step4` calling `bio::app::step4_weidu_log_export::auto_save_step4_weidu_logs`). BIO's `page_step2` / `page_step3` / `content_step3` / `render_toolbar` / `page_step4` are **not** called by the workspace step router (the legacy `BIO` binary still renders its own). Workspace state loader populates the orchestrator's owned `WizardState` from `modlists/<id>/workspace.json` on workspace open and extracts back on nav-away / debounce / on-exit. Phase 6 closed after the user verified the Phase-6 plate (FR2-4 Create-screen one-pass + #2 grey-bar-under-GameTab punted to a separate Phase-8 pass).

**Task IDs (full detail in archive):** P6.T1, P6.T2, P6.T2b, P6.T2c, P6.T2d, P6.T3, P6.T4, P6.T5, P6.T6, P6.T7, P6.T8, P6.T9, P6.T10, P6.T11, P6.T12, P6.T13, P6.T14, P6.T15.
