# Infinity Orchestrator — Plan Decision Log (frozen reference)

**Frozen historical reference — not a live log.** This file once held the full dated decision history through 2026-05-21. It has been slimmed to only the entries the live plan still links: the C1–C5 + H-tier definitions that `overview.md` points into (C4 / C5 / H2 / H3), and the Phase-6 / Phase-8 rationale entries the phase docs cite. The complete pre-slim history is preserved in git — `git show <older-commit>:infinity_orchestrator/plan/revision-log.md`. Decisions from 2026-05-23 onward (the Phase-8 arc, the binary rename, the branding pass) live in the merged PRs and commit messages, not here.

Back-pointer: [`overview.md`](overview.md).

### 2026-05-12 — C1/C2/C3/C4/C5 + H-tier collateral pass (this revision)

**Scope:** Address the five remaining critical findings from the adversarial review (C1 visibility/access, C2 WizardApp→WizardState audit, C3 errors_detected fiction, C4 Step 4 body replacement, C5 rail-nav lock) plus the H-tier collateral fixes that fall out naturally. The user approved all resolution paths.

**Carve-outs the SPEC now authorizes (SPEC §1 CRITICAL DIRECTIVE, expanded):**

1. **#1 Theme-token extraction** (pre-existing).
2. **#2 Window-chrome config flips** (pre-existing).
3. **#3 Library/binary structural split** (NEW — authorizes the `src/lib.rs` + slim `src/main.rs` + `src/bin/infinity_orchestrator.rs` restructure).
4. **#4 WizardApp → WizardState signature refactor** (NEW — authorizes refactoring BIO functions whose mutation surface is exclusively `app.state` to take `&mut WizardState` directly).

**Changes applied in this revision:**

- **C1 — Library/binary split** (Phase 1).
  - `src/lib.rs` declares the entire BIO module tree as `pub mod app; pub mod ui; pub mod settings; pub mod logging; pub mod cli; pub mod config; pub mod install; pub mod mods; pub mod parser; pub mod platform_defaults;` (mirroring today's `src/main.rs` `mod` block but with `pub` visibility).
  - `src/main.rs` becomes a 12-line shim that calls `bio::ui::run` / `bio::app::dispatch::run`. CLI parsing logic is preserved verbatim; only the `crate::` → `bio::` path rewrite changes.
  - `src/bin/infinity_orchestrator.rs` calls into `bio::ui::orchestrator::OrchestratorApp::new(...)`.
  - `Cargo.toml` gains a `[lib] name = "bio"` block and the `[[bin]] name = "BIO"` block. The existing `[[bin]] name = "BIO_legacy"` block stays.
  - Result: orchestrator code lives inside the library crate at `bio::ui::orchestrator::*`, so `pub(crate)` BIO items remain reachable.

- **C2 — WizardApp → WizardState signature refactor audit** (Phase 4, Phase 6, Phase 7).
  - The audit table (with verification commands and outcome per function) lives in each relevant phase. Summary:
    - `src/ui/app_step2_log.rs::apply_weidu_log_selection(app: &mut WizardApp, bgee: bool)` — **Stays as `&mut WizardApp`**. Body calls `app.save_settings_best_effort()` (a `WizardApp` method), so the function touches more than `app.state`. The orchestrator builds a net-new sibling that calls the underlying `bio::app::app_step2_log::apply_weidu_log_selection_from_path(&mut state, bgee, log_path)` (`pub fn`) directly without going through `WizardApp`.
    - `src/ui/app_methods.rs::handle_step1_action` — **Stays as `&mut WizardApp`**. Body mutates `self.step1_github_auth_rx` (a channel receiver, not in `state`). Orchestrator builds a net-new equivalent in `src/ui/settings/oauth_glue.rs` that owns its own receiver field on `OrchestratorApp`.
    - `src/ui/app_methods.rs::handle_step2_action` — **Stays as `&mut WizardApp`**. Body calls `super::step2_router::handle_step2_action(self, action)` which itself takes `&mut WizardApp` because it mutates `app.step2_scan_rx`, `step2_cancel`, `step2_progress_queue`, etc. (channel receivers). Orchestrator dispatches actions through its own equivalent that calls `bio::app::app_step2_router::handle_step2_action(&mut state, &mut scan_rx, &mut cancel, ..., action)` (the underlying `pub(crate) fn` per `src/core/app/app_step2_router.rs:6`) directly — `bio::app::app_step2_router::handle_step2_action` already takes only state + per-receiver `&mut` args, no `WizardApp`.
    - `src/ui/app_methods.rs::handle_step4_action` — **Refactor authorized but unnecessary**. Body calls `super::step4_flow::handle_step4_action(&mut self.state, action)`, which is just a re-export of `bio::app::app_step4_flow::handle_step4_action(state: &mut WizardState, action: Step4Action)` — that public `pub(crate) fn` per `src/core/app/app_step4_flow.rs:8` already takes `&mut WizardState`. The orchestrator calls it directly. No carve-out #4 refactor needed.
  - **Net effect:** no BIO functions are refactored under carve-out #4 in this pass. The audit found that every candidate either (a) genuinely needs `WizardApp` fields beyond `state` (channels), or (b) already takes `&mut WizardState` at the underlying `bio::app::*` layer and can be called directly. The carve-out remains available for future cases.

- **C3 — Replace `errors_detected` with real BIO success-detection fields** (Phase 7).
  - **Verified via `src/core/app/state/state_step5.rs:12-66`:** `Step5State` has no `errors_detected` field. The real success-detection fields are:
    - `pub last_install_failed: bool` (set in `src/core/app/step5_runtime_status.rs:72` from `term.likely_failure_visible()` on exit).
    - `pub last_exit_code: Option<i32>` (set in `src/core/app/step5_runtime_status.rs:73`).
    - `pub install_running: bool` (toggled false on exit at `src/core/app/step5_runtime_status.rs:74`).
    - `pub has_run_once: bool`.
  - **BIO's canonical "clean install" check** (per `src/ui/step5/content/content_install_row_step5.rs:115-117`): `state.step5.has_run_once && !state.step5.install_running && state.step5.last_exit_code == Some(0)`. This is the gate BIO uses for its own "Export Modlist…" button.
  - **Replacement everywhere the plan said `errors_detected`:** the condition becomes `state.step5.install_running == false && state.step5.last_exit_code == Some(0) && state.step5.last_install_failed == false`. The phrase "clean exit" in the plan maps to this triple.
  - Updated tasks in Phase 7: P7.T4 (success-banner gate), P7.T6 (`flip_to_installed` trigger), P7.T9 (concurrency gate's `install_in_progress` check — uses `install_running`), and the post-install action row visibility (P7.T5).

- **C4 — Replace BIO's Step 4 body with an orchestrator-side renderer** (Phase 6, P6.T2b rewritten).
  - The orchestrator's Step 4 wrapper (`src/ui/workspace/step4/workspace_step4.rs`) renders the entire Step 4 panel in new code: top action row (Save button + count), tab row (game tabs), and the line-numbered monospace review list per SPEC §8.1.
  - **The Save button calls BIO's existing public save logic.** Verified: `bio::app::step4_weidu_log_export::auto_save_step4_weidu_logs(state: &mut WizardState) -> Result<(), String>` is `pub(crate) fn` per `src/core/app/step4_weidu_log_export.rs:50` and takes `&mut WizardState` directly. The orchestrator calls it without any C2-style refactor. (BIO's `bio::app::app_step4_flow::handle_step4_action` wraps it but the orchestrator reaches the underlying function directly — both are reachable from same-crate orchestrator code per the C1 split.)
  - BIO's `bio::ui::step4::page_step4::render` is **not called** from the workspace step router for v1 alpha. (Today's BIO continues to invoke it from `WizardApp`'s update loop; the orchestrator just doesn't.)
  - The line-numbered monospace list reads from `wizard_state.step3.bgee_items` / `wizard_state.step3.bg2ee_items` — verified `pub` fields per `src/core/app/state/state_step3.rs:23-24`, each a `Vec<Step3ItemState>`. The wrapper uses the same three-color WeiDU-line syntax per SPEC §6.7 — implemented in `src/ui/workspace/widgets/weidu_line.rs`.
  - Exact-log mode: when `install_mode == install_exactly_from_weidu_logs`, the wrapper renders the read-only viewer + Check Mod List button per SPEC §8.2 / A.7. The Check Mod List button triggers `bio::app::app_step4_flow::handle_step4_action(state, Step4Action::CheckMissingMods)` — `pub(crate) fn`, reachable same-crate.
  - SPEC §8.1 / §8.2 BIO-fidelity callouts for the save action stay (the save logic is reused via the public `auto_save_step4_weidu_logs`); the rendering surface is **net-new**, not BIO-fidelity. Phase 6's P6.T2b reflects this.

- **C5 — Rail-navigation lock during in-flight install** (Phase 7, new P7.T9b).
  - When `install_runtime::install_concurrency::install_in_progress(orchestrator)` returns `Some(RunningInstall { modlist_id, .. })`, every rail item except the currently-active modlist's workspace is disabled with the SPEC §13.15 tooltip.
  - Home / Install / Create / Settings rail buttons all show the tooltip and are non-clickable during an install.
  - The user can stay in the running install's workspace and watch it; they cannot navigate away. To navigate away they must cancel the install first.
  - **Workspace state loader is never invoked while an install is running** — eliminating the swap-mid-install corruption risk.
  - Phase 7's Open Questions are updated to remove the C5 risk language. The `registry_snapshot` mitigation from H8 is **dropped** (it was protecting the wrong thing — the rail-nav lock makes it moot).

**H-tier collateral fixes applied:**

- **H2** — Step 3's `page_step3::render` returns `()`, not `Option<Step3Action>`. Verified at `src/ui/step3/page_step3.rs:7-9`. The plan's `step_action_dispatch.rs` and Phase 6's P6.T2 are updated to skip the Step 3 dispatch arm (the page handles its actions internally; no action enum bubbles up).
- **H3** — `crate::ui::app_update_loop` is **not** a public module. The real path is `bio::ui::app::update_loop`, which is a `mod` (private) inside `src/ui/app.rs` (declared at `src/ui/app.rs:31-32` via `#[path = "app_update_loop.rs"] mod update_loop;`). Plan references that previously read "see `app_update_loop::run` for the dispatch surface" now correctly cite `bio::ui::app::update_loop::run` and clarify it as a **read-only reference path** — the orchestrator never calls it.
- **H4** — Persistence-on-exit uses `eframe::App::on_exit` as the primary hook + `Drop for OrchestratorApp` as the fallback. Documented in Phase 3 P3.T7 and Phase 6 P6.T15.
- **H5** — The orchestrator's `OrchestratorApp::new(dev_mode)` calls `bio::app::app_bootstrap_init::initialize(dev_mode)` directly (now reachable via lib split + same-crate orchestrator). `initialize` is `pub(crate) fn` per `src/core/app/app_bootstrap_init.rs:17`. No "copy the relevant logic" fallback needed. Documented in Phase 2 P2.T3.
- **H7** — `install_redesign_fonts` builds a complete `FontDefinitions` from scratch (no additive composition on top of `configure_typography`'s output). Phase 1 P1.T1 updated: the new function calls `egui::FontDefinitions::default()`, registers `poppins_light` / `poppins_medium` / `poppins_bold` / `firacode_nerd`, and replaces the proportional + monospace families with these new ones. `configure_typography` then runs on the already-installed font definitions (it only mutates `Style`, not `FontDefinitions`). The two functions compose correctly with this ordering: call `install_redesign_fonts` first, then `configure_typography`.
- **H8** — The Phase 7 `registry_snapshot` mitigation is **dropped**. C5's rail-nav lock makes mid-install registry corruption moot (the user cannot navigate the orchestrator while an install runs; external mutation to `modlists.json` is out-of-scope for v1 alpha). Phase 7 P7.T3 simplified.
- **H9** — Step 5's post-install buttons (`Return to Home`, `Open install folder`) are rendered **immediately above** BIO's embedded Step 5 panel, not below. Per SPEC §9.2 "next to the disabled Install button." BIO's `page_step5::render` puts the Install button at the top of its panel; the orchestrator's chrome row above the panel sits visually adjacent to it. Phase 7 P7.T5 updated. The alternative (replacing BIO's Step 5 body wholesale per C4) was rejected as too heavy for v1 alpha.
- **H10** — Wireframe label is "Clear contents", not "Replace contents". Phase 5 P5.T9 updated.
- **H11** — Per-edit debounced path validation is a real Phase 4 task (P4.T11b), not just an Open Question. Debounce window 200ms idle per keystroke (authoritative; matches SPEC §11.2 and `validate_debounce::DEBOUNCE_MS`).

**What remains open (not addressed in this pass):**

- M/L items from the adversarial review (a later polish pass).
- Real game launcher for the Home `play` button (out of scope for v1 alpha; opens install folder as fallback).
- Real Nexus Mods / Mega OAuth integration (out of scope for v1 alpha; stubbed).
- The Explore tab (SPEC v2 track, not v1 alpha).
- ~~Step 2 / Step 3 / Step 4 details panel and tree theme-token extraction (Phase 8 carve-out audit; current Phase 8 scope only covers popups and Step 5 console line tones).~~ **Now covered.** Phase 8 has been rewritten to cover the full visual-fidelity sweep under carve-out #6 (state-aware theme-token reads): Step 2 tree + Details, Step 3 reorder list + toolbar, Step 5 console + status + install row + dev header + cancel-confirm, plus the popup group already in pre-revision scope.

### 2026-05-17 — Step-3 C4 chrome SHIPPED (P6.T2d) + the real SPEC/plan/HANDOFF cascade landed *with* the code

The proper close of the Step-3 C4 chrome gap (previously recorded as OPEN): a scoped `plan-implementer` run that ships the working screen **and** the docs together, docs reflecting shipped reality (never ahead of it).

**(1) Net-new Step-3 C4 chrome — the direct analogue of the shipped Step-2 (`workspace_step2`) / Step-4 (`workspace_step4`) C4.** New `src/ui/workspace/step3/` (`mod.rs` + `workspace_step3.rs` + `step3_action_row.rs` + `step3_tab_row.rs`). `workspace_step3::render(ui, orchestrator) -> ()` (H2). The reused-BIO seam matches `content_step3::render` (`content_step3.rs:224-261`) **exactly**: `state_step3::normalize_active_tab` → `toolbar_support_step3::build_toolbar_summary` (`pub(crate)`, reachable via the carve-out #3 lib+bin split — no PLAN GAP on the seam) → set `step3.{bgee,bg2ee}_has_conflict` (the list body tones rows off these) → `bio::ui::step3::list_step3::render(ui, state, &mut jump, markers)` (`pub(crate)`) inside an orchestrator-owned **hard-clipped fixed-size rect** (the verbatim Step-2 `clipped_pane`, so the list — which paints its own `ui.group` + scroll — can never bleed into the workspace nav bar; jump-flag round-trip mirrors `content_step3:254-260`) → `content_step2::render_compat_popup` + `prompt_popup_step2::render_prompt_popup`. Net-new chrome above it: the action-row count "_N_ components ready to install on _<tab>_ · across _M_ mods" (SPEC §7.1; reuses the shared `pub` `workspace_step4::active_tab_items` resolver so Step-3 / Step-4 never drift; no Save button — Save is Step-4-only, §7.6) + the redesign tab row = the **shared** `widgets::game_tab::game_tab` GameTabs (reused verbatim — Steps 2/3/4 render the one widget identically, the 2026-05-17 unification; single-game skips the strip via `workspace_step4::is_dual_game`) + the aggregate conflict / prompt clickable `Pill`s + redesign `Undo`/`Redo`/`Collapse All`/`Expand All`. Every interactive control delegates to the **exact** `pub(crate)` helper `content_step3::render_toolbar` itself calls (`toolbar_support_step3::{open_toolbar_issue_popup,undo_active,redo_active,collapse_all_active,expand_all_active}` + `prompt_popup_step2::open_toolbar_prompt_popup`) — read-only, **no behavior change** (drag/undo/collapse stay BIO's). **Curated exactly as Step-2's C4 dropped its dev button:** **no** "Export diagnostics" / "Restart App With Diagnostics" (BIO paints those only in `render_toolbar`, never called → structurally absent by construction), **no** BIO heading/hint (the workspace shell renders the per-step hint via `workspace_hint_line` — the Step-2 #6 precedent). The **only** non-net-new edit is the Step-3 arm of the redesign-owned `workspace_step_router.rs` (`page_step3::render` → `workspace_step3::render`); `src/ui/workspace/mod.rs` gains an additive `pub mod step3;` (redesign-owned, not a carve-out). `step3_tab_row::render` is `pub(crate)` (not `pub` like the Step-2/4 split helpers) because BIO's `Step3ToolbarSummary` is `pub(crate)` — a `pub` signature would leak a more-private type (`private_interfaces`); same-crate-only caller ⇒ `pub(crate)` is idiomatic and behaviour-identical. **Wireframe vs SPEC reconciled:** `screens.jsx::ComponentsPanel` draws no action-row count (only Step-4's `OrderPanel` does), but SPEC §7.1 explicitly specifies it for Step 3 — SPEC prose wins where the wireframe is silent (spec-authority order); recorded so a future review doesn't re-flag it.

**(2) The real doc cascade — landed WITH the code, past-tense "shipped".** SPEC §7.1 gained the C4-chrome callout (mirroring how §6 documents Step-2's reused-vs-net-new split + the shell-renders-the-hint precedent). phase-06 re-added **P6.T2d** as the *implemented* work order (mirrors P6.T2c incl. acceptance) + flipped every Step-3 pointer (:7 summary, :29/:181 "what ships", the P6.T2 router body+prose+acceptance, the "Step 3 — no action enum" sub-section, the dispatch-inventory comment, the file-inventory `mod.rs`/router/`page_step3` rows + new `step3/` rows) to "Step 3 = `workspace_step3::render` (C4)". phase-08: the `content_step3.rs` carve-out #6 row **struck** (the orchestrator no longer calls it — Step-3's carve-out #6 reduces to the reused `list_rows_step3.rs` row only) + P8.T5 retitled/rescoped + the re-add-prose + the verification step 6 corrected. HANDOFF: the Phase-6 Step-3 line rewritten, the C4 review entry extended to Step 3 (**SHIPPED**), the Phase-6 status row noting Step-3 C4 done. This entry is the decision-log record.

**Verification (orchestrator-independent — the orchestrator re-verifies before commit).** BIO-source guard empty: the only code changes are `src/ui/workspace/step3/**` (net-new), `src/ui/workspace/mod.rs` (+`pub mod step3;`), `src/ui/workspace/workspace_step_router.rs` (the one authorized redesign-owned Step-3-arm edit) — `git status` grep for protected BIO paths returned empty; protected `content_step3`/`page_step3`/`list_step3`/`toolbar_support_step3` provably **untouched** (grep-confirmed; reused read-only). C4 boundary grep-proven: `page_step3::render` has exactly one executable call site — `src/ui/app_update_loop.rs:38` (the **legacy `BIO` binary**, correct/unaffected); `content_step3::render` only `frame_step3.rs:9` (BIO-internal); `render_toolbar` zero call sites — **zero orchestrator call sites of all three**, exactly as Step-2/Step-4. `cargo build --bin BIO_legacy --release` **compiles fully clean** (only 2 pre-existing BIO `terminal` warnings; my one transient `private_interfaces` warning fixed via the `pub(crate)` visibility) but the final exe relink was blocked — a process holding the exe path was detected (the legitimate file-lock gotcha; reported honestly, **NOT** claimed fresh — the orchestrator/user must close it and re-run the link, then prove freshness via a confirmed no-op build). `cargo build --bin BIO_legacy --release` `Finished` (legacy binary clean). `cargo test --lib` **233 passed; 0 failed** (the 2 new `step3_action_row` tests use `WizardState::default()` only — pure in-memory, no `RegistryStore::new_default()`); the directive-grade DATA-LOSS guard held: `%APPDATA%\bio\modlists.json` SHA256 byte-**identical** before/after the suite (`841D109F…A30902`, 1146 bytes). Implementer reported; the orchestrator independently re-verifies, commits (Xgatt-only), runs the final pre-handoff rebuild gate (which MUST end in a confirmed no-op once the exe lock is cleared), and hands to the user for visual sign-off.

### 2026-05-17 — Fix-Run-2 escalations RESOLVED (user decision): both deferred to Phase 8

The orchestrator premise-checked both escalations TRUE before relaying (§6.7 is literally "Step 3 / Step 4 only"; `prompt_popup_step2.rs:31` is protected BIO with no carve-out). User (final authority) decision: **both → Phase 8, recorded in `plan/phase-08-popup-reskins-polish.md` deferred backlog.** (1) **#7b** (fork/import preview weidu 3-hue) is a colours-only change fitting the Phase-8 UI-colour reskin; the shared `preview_tabs` renderer + the §6.7 "Step 3/4 only" scope are reconciled *at Phase 8* (extend §6.7 then, applied consistently incl. the Install preview) — no SPEC change now, §6.7 stays accurate. (2) **#4a** (prompt-popup growth) is a small targeted BIO bug fix; **Phase 8 likely introduces a NEW SPEC §1 carve-out for important-but-small BIO bug fixes**, this being the first candidate (the ~1-line bounded-size change in the Text branch) — the carve-out is authored at Phase 8, not now. `PENDING_VERIFICATION.md` FR2-9 flipped from "awaiting decision" to "Phase-8-deferred, do not re-flag/test here". Fix-Run 2 (commit `41d70f7`) thereby fully closed; zero open Phase-6 escalations.

### 2026-05-18 — Verification-process fix: egui_kittest rendered-snapshot gate + Fix-Run 6 (Create layout actually correct, orchestrator-verified by looking)

**Root process failure named + fixed.** Across ~5 UI rounds the orchestrator "verified" Create fixes via scope/BIO-guard/`cargo test --lib`/diff-matches-the-settled-design and shipped visibly-broken layouts every time (Fix-Run-5 even regressed: right margin gone, `Start →` clipped, footer jammed under content). Those gates are structurally blind to pixels; "diff matches the settled design" is hollow when the design was authored without rendering; the user was the sole renderer every round and rightly lost confidence. **Fix:** a headless **`egui_kittest`** rendered-snapshot gate — committed `7086149` (`tests/ui_snapshot_create.rs`, dev-dependency only, egui 0.31.1/patch untouched; spike proved feasible on this Windows env with no wgpu config). Recorded as a standing directive (`feedback-ui-render-gate` memory + `MEMORY.md`): UI-layout changes are verified by a rendered PNG **the orchestrator personally opens and judges**, full-shell + multi-width, before any handoff — never diff/build/test alone.

**Fix-Run 6 (`e305f92`) — the Create #4 cluster actually fixed, each verified against renders.** Gate first made **shell-faithful** (reproduces the live titlebar 34 + statusbar 26 + rail 200 + CentralPanel{28} scaffold structurally, NO `OrchestratorApp`/store — DATA-LOSS-safe — so margin/clip bugs are in-frame; the prior bare-content-rect gate could not see them). Then `stage_choose.rs`: **P1** footer truly bottom-pinned (the real fork-stage precedent = natural-height body + flexible spacer + footer flush; the Fix-Run-5 "allocate reserves height" assumption was false — egui advances by `min_rect`); **P2** right-margin-collapse root cause = the game `ComboBox` had no `.width()` so it fell back to ~combo_width+margin, overflowed `RIGHT_COL_W_PX`, expanded the page `max_rect` and ate the 28px right gutter / clipped `Start →` — fixed by pinning the ComboBox to the shared column width; **P3** shared `FORM_ROW_GAP_PX` ⇒ modlist-name input == destination input width and game-slot right edge == `browse…` right edge in BOTH states (import "imported" now fills the shared right-column box, not shrink-wrapped — reconciles #4a+#4g); **P4** selected-box text was washed out by the premultiplied near-opaque `redesign_selection_highlight` → replaced with an opaque `faint_accent_tint` (accent over shell-bg) so title/desc stay as legible as the unselected box while the accent border still signals selection (SPEC §5.1 "accent border + a faint accent tint", realized legibly); **P5** equal box height holds at 960. **No SPEC change** — P1–P5 *restore* already-specified §5.1 behavior (Fix-Run-5 already doc-synced §5.1 to footer-bottom-pinned + "imported" + accent-border-faint-tint; Fix-Run-6 makes the code finally match it); the two new constants are empirical px tuning (`feedback_wireframe_look_not_px`), not SPEC values; in-file doc-comments updated to not drift.

**Orchestrator-independent verification — including the part that cannot be delegated.** Only `src/ui/create/stage_choose.rs` + `tests/ui_snapshot_create.rs` changed; BIO-source guard empty; `cargo test --lib` 289/0 (287 +2 pinning tests); both binaries build; the snapshot test re-run under the orchestrator's own invocation regenerating 8 PNGs; **the orchestrator opened 1280/1045/1021/960 × scratch/import directly and confirmed every user-reported bug visually fixed** (footer flush-bottom; 28px right gutter at every narrow width incl. the user's exact 1045; modlist-name==destination width; game/browse one right edge in both states; selected-box text readable; equal boxes at 960). DATA-LOSS sentinel byte-identical (`modlists.json 3D8212D7`, `KRS5ZBMT0028 AE702C9A`, others unchanged). #8 forked share code was user-confirmed end-to-end earlier (fork lineage renders in fork info). Deferred/surfaced (NOT actioned): promote the gate to `try_snapshot` golden baselines once the user signs off (drift → CI fail); orphaned `forward_primary_button` dead-code (Phase-8 polish); save-model UX redesign + deselect-last (already deferred). Next: the user's live visual sign-off on the gate-fresh binary, then Phase 7.

### 2026-05-18 — Phase-6 verification plate CLEARED: #1/#3/#4 user-verified→closed; FR2-4 + #2 punted to the Phase-8 one-pass (user)

User decision 2026-05-18 (initial statement then corrected — recorded as resolved): **#1 Workspace Step-2 content alignment (`ab4453b`), #3 workspace rename-pencil glyph (`ab4453b`), #4 Step-3 C4 chrome (`fad78c3`) are user-verified → Confirmed closed**, closed out in Phase 6 (no re-test). **FR2-4 (the Create full one-pass; render-verified 1280/1045/1021/960 × scratch/import via the egui_kittest gate, Fix-Run-5/6) and #2 are punted to the Phase-8 verification one-pass** — the Phase-6 fixes are shipped and **NOT reverted**; only the live visual sign-off moves. (Correction trail: the user first grouped "#1 and #2" as punted, then corrected — #1 is verified/closed, #2 is the punted one.) **#2's real root cause, per the user:** the GameTab widget's own bottom bar was already removed in Phase 6; the residual symptom is a *grey-bar appearance under the tab strip caused by the reused BIO content pane below painting a grey top edge* against the redesign chrome — a reused-BIO colour issue squarely in the **Phase-8 carve-out #6 "BIO grey pane-border recolor"** deferred-backlog surface. **Phase-6 verification plate is now clear**; next user verification event = the Phase-7 phase-end pass (FR2-4/#2 are a separate later Phase-8 pass). Doc-sync (keep-SPEC/plan/HANDOFF-in-sync): `PENDING_VERIFICATION.md` rewritten (open items → none; #1/#3/#4 → Confirmed-closed with the session sign-off as evidence; FR2-4/#2 → Deferred-to-Phase-8 with plan back-references); the Phase-8 plan's deferred-backlog item 1 (BIO grey pane-border recolor, carve-out #6) **sharpened with the concrete #2 instance** (the Step-2/3/4 content-pane grey-top under the GameTab strip) so the punt is plan-backed and concrete when scoped; the orchestrator's runbook "Pending user verification" block updated to the cleared state. Orchestrator note: the Phase-8 plan already documented the Create-screen cleanup (FR2-4) and a generic grey-pane-border backlog item (#2's category) — verified by reading the plan end-to-end before recording (`feedback_orchestrator_full_knowledge`); the only addition was making #2's symptom concrete, not inventing scope. No code change; no SPEC change; branch `overhaul/infinity_orchestrator`.
