# Phase 5 — Home + Install Modlist (paste / preview / download stages)

**Status:** SHIPPED — full audit record: [`archive/phase-05-home-install-paste.md`](archive/phase-05-home-install-paste.md).

**Commit anchors:** `35881ca` "Phase 5 prep: ModlistEntry.paused_at_step + clipboard decision", `b8ab602` "Phase 5 Run 1: Home — visual + navigation" (+ Run-1 follow-ups `9f134a3` / `e11ffd3` / `c7500f5` / `a1c7249` / `37df60e` / `828f2d4`), `bea8ccc` "Phase 5 Run 2: Home — actions live" (+ `4afe617`), `8f00bff` "Phase 5 Run 3: Install — shell + paste stage + stage-4 stub" (+ `87763ce` / `4a9c037` / `0894223`), `57fb1bd` "Spec/plan: pack provenance trio into share code + ForkInfoPopup (carve-out #5)" + `acff12f` adversarial-review fix-up, `685981e` "Phase 5 Run 4: Install Preview — provenance display + allow_auto_install gate + ForkInfoPopup (P5.T10/T11)" (+ `a6005b2` follow-up), `afe79a8` "Phase 5 Run 5: Install Downloading §4.3 chassis + SPEC §13.12a; Phase 5 complete" + `c989f0d` plan-sync sweep, `f679910` / `f8b2351` / `ae51161` Run-5 QA, `32781d2` Run-5 adversarial-review QA.

**What shipped:** the real Home screen (filter chips `Installed (N)` / `In progress (P)` / `All (N+P)`, modlist cards with Resume/Play + Kebab actions, Add-a-modlist Box, game-installs-detected block reading Phase-4 path-validation events, first-launch empty state, delete confirm dialog removing both registry entry and install folder) and the Install Modlist destination's first three stages: paste + preview fully (reusing BIO's `preview_modlist_share_code` share-code parser), plus the §4.3 Downloading **chassis only** — built / navigable / forward-compatible (the live download/extract data + per-install dirs + content-addressed staging is Phase 7 P7.T17 / SPEC §13.12a, since that pipeline terminates in the install runtime). Carve-out #5 extended from one to four schema-additive `ModlistSharePayload` fields (`allow_auto_install` + the provenance trio `name` / `author` / `forked_from`); §10.9 ForkInfoPopup wired. The wireframe `play` label is rendered as `open` (M6) until a real game launcher ships. Stage 4 stub showing "Install runtime — Phase 7".

**Task IDs (full detail in archive):** P5.T1, P5.T2, P5.T3, P5.T4, P5.T5, P5.T6, P5.T7, P5.T8, P5.T9, P5.T10, P5.T11, P5.T12, P5.T13, P5.T14, P5.T15, P5.T16, P5.T17, P5.T18.
