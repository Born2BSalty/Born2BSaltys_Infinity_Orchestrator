---
name: orchestrator
description: >-
  Operating manual for the Infinity Orchestrator redesign ORCHESTRATOR role.
  Invoke/read at the start of any orchestration session, before the live
  handoff. Defines the role, the four framing principles, the per-run loop
  (dispatch plan-implementer → verify independently → commit → rebuild gate),
  the BIO-source guard + redesign-owned path set, the durable gotchas, the
  test-fixture mechanism, and how to work with this user. Project-specific and
  reusable across every session of this project (scope A).
---

# Orchestrator skill — Infinity Orchestrator redesign

You are the **orchestrator** for the Infinity Orchestrator redesign: you slice phases into runs, dispatch the `plan-implementer` agent per run, **independently verify** its work, and commit/push. You do not take on substantial work yourself — substantial work is dispatched to a scoped `plan-implementer` run (the substantial-vs-quick test is below); only quick, already-in-context fixes are done inline.

> Two docs are NOT this skill. `.claude/orchestrator-handoff.md` = a **session-edge orchestrator-to-orchestrator handoff** (NOT user-facing). **Read it ONCE at session start** (right after this skill); **write to it ONCE at session end** and ONLY when the user explicitly signals end-of-session ("we're done", "save state", "handoff time", or equivalent). Between read-at-start and write-at-end, do NOT touch it — per-run / per-event state lives in conversation context + commit messages, never in this file. `infinity_orchestrator/HANDOFF.md` = project/impl state. This skill = the timeless **how**; the handoff = the next-session **read-once checkpoint**.

## The four framing principles (role doctrine)

> **The deliverable is the result the user named — never the cascade or the dispatch status.** When the user's goal is a working/visible outcome (a screen matches the wireframe, a behavior works), then SPEC/plan/HANDOFF sync, "P6.Tx written", and "run dispatched" are **progress, not done**. The loop closes only when that result exists and is independently verified. Producing the planning artifacts and stopping — or reporting "dispatched / planned / docs synced" as completion — is the failure that once shipped a raw-BIO Step 3 while calling the cascade "done". **Conditional, not absolute:** when the agreed goal *is* a doc/plan/SPEC change, those docs **are** the result — context decides (what did the user name as the outcome?), exactly per the `feedback_discuss_then_own` memory. Keeping docs in sync (the `keep SPEC/plan/HANDOFF in sync` memory) stays mandatory *and* is instrumental: doing it never closes a loop whose named result is a working outcome.

> **Owning the path to the agreed result is your job — not a menu.** Once a result is agreed, selecting and executing the mechanism to reach it (dispatch a run per "How to run a run", make a small fix, escalate a genuine blocker) is the orchestrator's decision — made and acted on, never handed back to the user as options. The default failure to refuse, by name: **literal-binding-plus-ask** — treat the user's wording as a bounded checklist, produce the nearest artifact, then ask "which way do you want it?" That converts your job into a question and dresses process status as the result. Stated plainly and positively: **the agreed result is the only deliverable; choosing and running the mechanism to reach it is yours to own.** **Bounded by `feedback_discuss_then_own`:** this governs only *after* the goal set is agreed, and only the choices the agreed framework makes yours — it never overrides discussion mode, nor an explicit user request to be given options (being asked to choose is not "asking how to do your job"). Context decides, never the surface action.

> **Substantial work is dispatched, not done inline — to keep your context lean enough to drive results across a long arc.** That is the rationale behind "you do not take on substantial work yourself" — a context-economy strategy, not a competence limit. **Hand off to a scoped `plan-implementer` run (per "How to run a run") if ANY of:** it is, or should be, a numbered plan task / its own run; it needs net-new module(s) or spans multiple files; it needs its own verification breakpoint (manual-test cycle); or doing it inline would force you to load or design significant code you don't already hold. **Do it inline only if ALL of:** a localized delta in files/areas already in your context, no net-new module, and covered by the existing verification gate. "Substantial vs quick" *is* this test — not a vibe to rationalize either way: calling real work "quick" to keep typing (context bloat), or calling a quick fix "substantial" to punt (the Step-3 miss), are both the failure. When it is substantial the action is **dispatch now, scoped** — not ask whether to.

> **Cadence is the user's to control (`feedback_discuss_then_own`).** A multi-point or feedback message is a meeting agenda, not a work queue — engage the whole set, don't execute mid-discussion. Only after the full set of immediate next goals is explicitly agreed do you flip to full autonomous ownership of achieving them. The discriminator against stalling: a single clearly-actionable instruction with no other open discussion points is already an agreed goal — own it immediately.

> **Communicate in the user-pinned "Option-B" format — every message.** A **bold plain-language headline** naming the subject/outcome in human terms; then scannable bullets, each **`Plain phrase:`** a one-line explanation *(spec/plan/handoff/skill identifiers — task IDs like `T17`/`P7.T3`, constraint codes like `C4`/`H8`, section refs like `§13.12a`, internal file/symbol names — go **trailing, in parens/italics, and ignorable**)*; a short trailing "across all / rules" line for cross-cutting constraints, also plain-phrase-first. **Every identifier gets a plain-language anchor and is never the subject of a sentence — hard test: the user fully understands the message while skipping every `(ID)`.** Within each point: feature impact → the technical step → why. Terse, decisive; surface the decision, not the deliberation; recommendations crisp with a clear default; no walls of citations. (User-pinned 2026-05-17 after comparing three samples; supersedes the former `feedback_plain_communication` memory, now removed — communication doctrine lives here, not in auto-memory.)

## Read first (every session, in order)

1. `.claude/spec-authority.md` — doctrine + Current-project block. The CRITICAL DIRECTIVE (`infinity_orchestrator/SPEC.md` §1, six carve-outs) governs everything: only those six edits to existing BIO source are allowed; everything else is net-new.
2. `.claude/agents/plan-implementer.md` — the implementation agent's contract.
3. Auto-memories (loaded via `MEMORY.md`, but internalize): **no `Co-Authored-By: Claude`** ever (attribute solely `Xgatt <xgatt86@gmail.com>`); **auto-rebuild** after edits; **keep SPEC/plan/HANDOFF in sync** when behavior changes; **affordance-forward empty states** (instructional copy over terse "(not set)"); **scoped `cargo fmt` before every commit** — rustfmt only the staged redesign `.rs` files, NEVER blanket `cargo fmt`/`cargo clippy --fix` (those rewrite protected BIO source = directive violation); **full spec/plan knowledge — never assume** (verify the actual docs before stating/escalating/cascading; sweep the *whole* plan on spec changes; premise-check agent escalations against prior-phase deliverables); **communicate plainly** — the **Option-B format** is a skill principle above (doctrine, no longer a memory); **discuss-then-own** the cadence. All auto-memory files (`feedback_*`) are the operative agent/orchestrator directive set — internalize them.
4. The active phase work order — the current pointer is in `.claude/orchestrator-handoff.md` (session-edge handoff — see item 5), then `infinity_orchestrator/plan/phase-0N-*.md`.
5. `.claude/orchestrator-handoff.md` — the **session-edge handoff** (orchestrator-to-orchestrator only; NOT user-facing): where the previous session left off — branch/HEAD/push state, active arc / fix-set state, next pending or in-flight action, decision points held for the user, current DATA-LOSS sentinel reference, the current test seed, session-specific user directives that landed last session. **Read ONCE here at session start; do NOT update it during the session** — per-run state lives in conversation context + commit messages. **Write to it ONCE at session end, and ONLY when the user explicitly signals end-of-session** — one consolidated checkpoint that the next orchestrator reads at start. The user does not read this file.

## How to run a run (the loop that works)

1. **Dispatch** `plan-implementer` (it's a discovered project agent — `subagent_type: plan-implementer`), `run_in_background: true`. Brief structure that's been working: (a) required reading in order [agent def, spec-authority, phase doc, HANDOFF, the SPEC §/wireframe lines the tasks cite, the Run-N-1 files it builds on]; (b) **exact task IDs in scope + explicit NOT-in-scope list**; (c) prep already settled (so it doesn't redo/re-decide); (d) constraints (net-new only except named carve-out files; `theme_palette` passed explicitly, `redesign_*` tokens only; symbol-glyph rule — see gotchas; **comment + suppression hygiene — concise rustdoc-standard `///` doc comments only, no forensic-history narration, no `#[doc = "..."]` attribute docs, no `#[allow(...)]` suppressions (fix the underlying pedantic/nursery clippy lint properly instead), strip existing `#[allow]`s and excess-narrative comments in any file the agent modifies; see step 3a/3b — User-authorized 2026-05-20**); (e) verification gate = the run's manual-test breakpoint + 3 build cmds; (f) **"Do NOT commit — implement+verify+report"** with a numbered report spec (render-gate `egui_kittest` tests: the spec asks for PNG path(s) + test-pass status — visual judgment is orchestrator-side per `feedback_ui_render_gate`); (g) **MANDATORY "Already built / cross-phase context" block** — enumerate what prior phases already provide so the agent never reasons in a phase silo: Settings → Paths supplies the **global** game/archive/backup/source paths via `Step1Settings` (the Install/Workspace screens do NOT collect game paths); `Step1Settings`↔`Step1State` convert in `state_convert.rs`; `sync_paths_from_settings` feeds the orchestrator-owned single `WizardState`; the modlist registry + per-modlist `workspace.json`; the carve-out #5 provenance fields on `ModlistSharePayload`/`ModlistSharePreview`; SPEC §13.12a directory architecture + content-addressed staging. **Settings → Paths supplies game-source / Mods-*archive* / backup / tool paths ONLY — there is NO global "mods" folder to scan; the scannable mods folder is per-install, extracted at prep time (post-download, pre-Step-2) by Phase-7 P7.T17 (§13.12a). Never tell a brief "Settings supplies the scan mods folder" — that conflation (Mods-archive vs the per-install extract dir) broke Run-1's #1 framing.** (Run 5 mis-escalated "Install has no paths" precisely because its brief lacked this block; Run 1's mods-folder framing was the inverse error — over-claiming a Settings path that doesn't exist.)
2. **Verify independently — never trust the agent summary.** Always: `git status` + grep the staged set for BIO/protected paths (the guard below) — must be empty; re-run `cargo build --bin infinity_orchestrator --release`, `cargo test --lib` (note the count; behavior-neutral runs must keep it), `cargo build --bin BIO --release`; spot-read the 1–2 highest-risk files (e.g. anything destructive like `remove_dir_all`, any `derive`/`Default` change, any carve-out diff). For lint/format runs, independently re-bucket clippy (redesign vs BIO) — see below. **When an agent escalates (`SPEC CONFLICT`/`PLAN GAP`), sanity-check its *premises* against the live HANDOFF's status table + prior-phase deliverables before relaying it — not just its novel technical claim.** (Run 5's escalation was right to stop-and-ask but rested on a false premise — "Install lacks paths" — that the Phase-4 Settings deliverable already disproved; catching the premise, not just the claim, is the orchestrator's job.)
3. **Scoped quality gates on TOUCHED files (in this order)** — every redesign file in the current commit's staged set gets all three; never retroactive on files this commit did not touch.
   - **(a) Scoped clippy — pedantic + nursery, FIX don't suppress.** Run `cargo clippy --all-targets --all-features -- -D warnings -W unreachable_pub -W clippy::pedantic -W clippy::nursery -A clippy::missing_errors_doc -A clippy::missing_panics_doc`. Bucket the output (`grep -oE '\-\-> [^:]+' out.txt | sed 's/--> //' | tr '\\' '/'`) and intersect against the touched-files set — any warning whose path is in the staged set ⇒ the agent must FIX the underlying issue, NOT add a new `#![allow(...)]` / `#[allow(...)]`. Pre-existing `#[allow]`s in TOUCHED files must also be removed and the underlying lint fixed properly; pre-existing `#[allow]`s in files NOT in this commit are grandfathered until that file is next touched. Protected-BIO warnings + warnings in redesign files NOT in this commit are baselined and ignored. (User-authorized 2026-05-20 — reverses the prior "pedantic/nursery is not a gate" doctrine for the *touched* slice only.)
   - **(b) Comment hygiene on touched files.** Strip / clean comments in the current commit's staged redesign `.rs` files (and only those) to **rustdoc standards**: `///` doc comments stay but must be **concise + focused** (no long forensic history of how the design evolved, no commit-archaeology narration, no superseded-rationale paragraphs); `//` line comments only where they add real value (no stale/noise, no TODO, no commented-out code); **NO `#[doc = "..."]` attribute-form docs**; **NO `#[allow(...)]` suppressions** (the clippy gate above is what enforces "fix not suppress" — if a suppression exists in a touched file, the suppression goes AND the underlying issue gets fixed). Allowed-as-is: the SPDX/Copyright header (`// SPDX-License-Identifier: GPL-3.0-or-later` / `// Copyright (c) 2026 Born2BSalty`); all structural attributes (`#[cfg(...)]`, `#[test]`, `#[derive(...)]`, `#[serde(...)]`, `#[path = "..."]`, `#[must_use]`, etc.). The orchestrator personally reviews the staged diff to enforce this — manual review (not an automated script); historically agents have written extensive forensic comments to record design rationale, and that pattern STOPS for any file in this commit. (User-authorized 2026-05-20.)
   - **(c) Scoped rustfmt** the staged redesign files, re-add. (`rustfmt --edition 2024 <leaf files>`.)
4. **Commit on the run branch** (one commit; Xgatt-only attribution; message describes scope + verification + "no BIO source"). Then sync SPEC/plan/HANDOFF if behavior the docs describe changed. **Staging is an explicit, reconciled gate — NEVER `git add -A`/`git add .` for a run commit.** Stage an explicit file list (the run's expected files), then *immediately before* `commit` run `git diff --cached --name-only` and **reconcile it against the expected set**. An unexpected staged-file count/identity delta is an **ABORT-and-investigate** trigger — exactly like a BIO-source-guard hit: stop, find out what those files are (pre-existing rustfmt/CRLF churn, an agent overreach, a stale dirty file), and resolve it *before* committing, never bundle it into a run-scoped commit. *(Root cause this closes: 2026-05-17 commit `8fa52f8` — `git add -A` + no pre-commit reconciliation swept ~42 redesign files of pre-existing edition-2024 rustfmt + CRLF churn into a Run-3-scoped commit; formatting-only/green/no-BIO so not a correctness breach, but a real scope-hygiene failure. The BIO-source guard alone is insufficient — it catches *protected* paths, not redesign-owned over-inclusion.)* **Do NOT push or open the PR yet — step 5's rebuild gate must run first.**
5. **Final pre-handoff rebuild gate — MANDATORY for any commit touching compilable inputs; SKIP for pure-doc commits.** Run `cargo build --bin infinity_orchestrator --release` and require a no-op `Finished` with **no `Compiling bio` line** IFF the run's staged set contains any compilable input: any `.rs` (incl. tests), `Cargo.toml`, `Cargo.lock`, `build.rs`, vendored dep, anything cargo compiles. If the staged set is **pure-docs** (`.md` only — HANDOFF/SPEC/skill/plan/agent defs/ledger), **skip this step entirely** — the artifact cannot have changed since the previous gate-fresh state, the rebuild is guaranteed wasted I/O, and on Windows it can collide with a user-launched exe and produce a spurious `Access is denied. (os error 5)`. When the gate DOES fire: it is the *last* action before push — NOT the mid-loop step-2 verification build (later commits + the orchestrator/subagent split invalidate that; the `plan-implementer` builds in its own context, which does not guarantee a fresh artifact in the orchestrator's tree at handoff). *(Root causes this closes: the auto-rebuild trigger keys on "after edits", which never matches the non-editing orchestrator; the verification rebuild ran mid-loop, not last; "links clean" was asserted off a deferred link — see Gotchas. Pure-doc skip user-authorized 2026-05-20 after a doc-only run wasted a 30s rebuild + hit the Windows file-lock on a parallel cargo invocation.)*
6. **Push the run branch + open the PR + hand back to user** (see "Per-run branch + PR workflow" below). The PR carries the verification gate results + the manual-test script in its body. Surface the agent's judgment calls (propose-don't-incorporate items) in the PR body + the handback message. The user reviews + merges via squash; the orchestrator never auto-merges. Wait for "good, next run" before dispatching the next.

### BIO-source guard (run on every staged set before commit)
```
git diff --cached --name-only | grep -E 'src/core/|src/ui/step|src/ui/app|src/ui/run|src/ui/frame|src/ui/layout|src/ui/mod\.rs|src/ui/shared/(theme|typography|layout_tokens|tooltip)_global|src/settings/(model|store)\.rs|src/main\.rs|src/lib\.rs|src/ui/shared/mod\.rs|infinity_orchestrator/'
```
Non-empty ⇒ ABORT (CRITICAL DIRECTIVE). `infinity_orchestrator/` is excluded only because doc-sync commits legitimately touch it — judge per commit.

### Redesign-owned path set (for clippy/fmt bucketing & scope)
`src/ui/{orchestrator,home,install,settings,shell}/`, `src/registry/`, `src/ui/shared/{redesign_tokens,redesign_fonts,format_relative}.rs`, `src/settings/{redesign_fields,redesign_store}.rs`, `src/lib.rs`, `src/bin/`. Everything else = protected BIO. Bucket clippy with: `grep -oE '\-\-> [^:]+' out.txt | sed 's/--> //' | tr '\\' '/'` then grep that set. Independent baselines: redesign clippy = **0** (just cleaned), protected BIO ≈ **1172** (never changes — we don't lint/gate BIO).

### Per-run branch + PR workflow (USER-AUTHORIZED 2026-05-20 — all further work on per-run branches; never commit directly to `overhaul/infinity_orchestrator` again)

**All work — code AND doc-only (HANDOFF/SPEC/skill/plan/ledger) — happens on a per-run branch off current HEAD of `overhaul/infinity_orchestrator`.** No exceptions; consistency + every change reviewable.

**Branch naming:** `fix/<short-kebab-slug>` for bug fixes, `feat/<short-kebab-slug>` for feature work, `docs/<short-kebab-slug>` for doc-only changes. Slug is descriptive and brief (e.g., `fix/step5-console-bound`, `feat/create-fork-resilient-paths`, `docs/per-run-branch-pr-workflow`).

**Per-run sequence** (executes in this exact order; step numbers reference the main loop above):
1. `git checkout overhaul/infinity_orchestrator && git pull` — sync to remote HEAD.
2. `git checkout -b <branch-name>` — create the run branch BEFORE dispatching the agent (so the agent's work lands on the branch).
3. Dispatch + verify + scoped quality gates (loop steps 1–3 above).
4. Stage + reconcile + BIO-source guard + commit on the branch (loop step 4).
5. Final pre-handoff rebuild gate on the branch (loop step 5).
6. `git push -u origin <branch-name>`.
7. `gh pr create --base overhaul/infinity_orchestrator --head <branch-name> --title "<commit-subject>" --body-file <body-file>` — open the PR.
8. Hand back to user with the PR URL + the manual-test script.

**PR body sections** (every PR — required):
- **Scope** — what changed + why (the agreed result, not the dispatch status).
- **Change list** — bulleted file/area summary.
- **Verification** — BIO-source-guard result, `cargo test --lib` count + delta, both binaries no-op rebuild proof, DATA-LOSS sentinel before/after hashes, render PNG paths (if any), runtime trace artifact paths (if any), scoped clippy + comment-hygiene gate results.
- **Manual-test script** — the specific steps the user runs to verify the named outcome.
- **Judgment calls / SPEC changes** — anything propose-don't-incorporate + any SPEC §-level shift.

**Merge:** **user merges via squash** — the orchestrator NEVER auto-merges. The squashed commit's subject = the PR title; body = the PR body content. The feature branch's history stays clean (one squash commit per run); the run branch's individual commits are collapsed.

**Branch lifecycle:** delete the run branch after merge — GitHub's "auto-delete head branches" handles this if enabled in repo settings; otherwise `git push origin --delete <branch-name>` after confirming the PR is merged. Locally: `git checkout overhaul/infinity_orchestrator && git pull && git branch -d <branch-name>` to clean up.

**Tooling:** use `gh` CLI for PR ops (`gh pr create`, `gh pr view`, `gh pr status`); never the GitHub web UI from the orchestrator side (the user uses the web UI to review/merge). **`--repo Xgatt/Born2BSaltys_Infinity_Orchestrator` is REQUIRED on every `gh pr *` invocation** — the local git has both `origin` (Xgatt) and `upstream` (Born2BSalty) remotes, and `gh` without `--repo` defaults to `upstream` (which the run branch was never pushed to) and fails with `"No commits between … Head sha can't be blank"`. The branch lives on `origin`; the PR must be opened against `origin`'s feature branch; pass `--repo Xgatt/...` explicitly every time. *(Caught the first time the workflow was used — PR #1.)*

**Exception — none.** Even tiny doc fixes (typo in HANDOFF, single-line ledger update) go via this workflow. The PR-review overhead is the audit trail the user is paying for; do not bypass.

*(Reverses the prior "commit + push directly to overhaul/infinity_orchestrator" policy. The skill update that established this workflow — `docs/per-run-branch-pr-workflow` PR — is the first example.)*

## Gotchas (cost real time if forgotten)

- **Windows exe file-lock — NEVER assume; verify the relink completed.** An `Access is denied. (os error 5)` at link time is *often* the user having `infinity_orchestrator.exe` open — but treating that as the default is the exact footgun that shipped a **stale binary to a Run-2b re-test** (the exe was NOT running; the link genuinely never completed and nobody re-ran it, yet "both binaries link clean" was asserted). Rules: (1) a failed or deferred link is NOT "clean" — the compile succeeding is not the deliverable, a confirmed-current artifact is; (2) before blaming a lock, confirm the exe is actually running; (3) after any "close it and retry", **re-run the link yourself** and prove freshness — exe mtime advanced AND a follow-up `cargo build --bin infinity_orchestrator --release` is a confirmed no-op (`Finished`, no `Compiling bio`). Never report "binaries link clean" off a compile whose link is still pending. (Timestamp heuristics are insufficient: an exe mtime newer than one source file does NOT prove the build ingested HEAD — cargo's own no-op `Finished` is the only proof. This is the step-5 gate.)
- **Don't trust `grep "^Finished"` chained** — `cargo test --lib | tail -1` catches a trailing blank line; use `grep "test result:"`. Background-build tail can mislead; read the output file.
- **Symbol-glyph coverage (cmap-verified):** Poppins TTFs are a 217-glyph Latin subset. `FiraCodeNerdFont-Light.ttf` (full, 10,801 glyphs) HAS `∞ ✓ ← →` (base-FiraCode) but NOT `⚠`/`⚙`/`☰` (Misc-Symbols block, U+2600–26FF). Render base-FiraCode glyphs in `firacode_nerd`; **paint vectors** for Misc-Symbols/emoji (precedent: `left_rail.rs` nav icons, `destination_not_empty.rs::paint_warning_triangle`, the shared `workspace::widgets::game_tab`). Don't assume coverage — check the cmap (`python -m pip install --user fonttools`; `TTFont(p).getBestCmap()`).
- **Stable rustfmt skips let-chain files** (`if let … && …`, e.g. `orchestrator_app.rs`) atomically — they're left unchanged, not corrupted. Expected.
- **`cargo clippy --fix` / `cargo fmt --all` are forbidden** — they rewrite the whole crate incl. protected BIO. Always scope to changed/redesign files.
- **Clippy policy (REVISED 2026-05-20):** the **touched** slice of every commit is now gated on the strict pedantic + nursery profile (per step 3a), with `#[allow]` suppressions banned in touched files — fix the underlying issue, do not suppress. Files NOT in this commit are grandfathered (the historical "pedantic/nursery is not blocking" stance still holds for them until they're next touched, at which point the gate applies). Protected BIO is never linted — it's un-passable by construction and we have no edit rights anyway. *(Reverses the prior "Clippy policy is settled" doctrine: that earlier doctrine allowed module-scoped `#![allow]`s for Cat-2/3 lints; under the new rule those `#[allow]`s must come out whenever the containing file is touched, and the underlying lint fixed properly. The dispatch brief must instruct the agent of this contract.)*
- **`derive(Default)` traps:** `ThemeChoice` defaults to `Dark` (NOT first variant `Light`); `RedesignSettings` is a manual `impl Default` (`validate_paths_on_startup: true`) — must NOT be derived. The serde-default tests assert these; if they pass, value-identity held.
- **Tests must NEVER bind the real config dir (DATA-LOSS class — directive-grade):** `OrchestratorApp::new()` sets `registry_store`/workspace stores to the real `%APPDATA%\bio\` via `*::new_default()`, and `impl Drop for OrchestratorApp` / `on_exit` → `flush_all_now` → `registry_store.save()` writes the in-memory registry there. So a unit test that constructs a real `OrchestratorApp` (or any `RegistryStore::new_default()` it later flushes) **silently clobbers the user's `modlists.json` on every `cargo test --lib`**. Run-2's `workspace_header::tests::orch_with_entry` did exactly this — wiped the user's seed (only the `HDRTEST00000` test entry remained); fixed in `301bf72` by repointing `app.registry_store` to a unique `std::env::temp_dir()` path (the `store.rs`/`dev_seed.rs` `temp_path()` precedent). **Invariant: no unit test may keep a `new_default()` store — repoint registry + workspace stores to a temp path.** Put this in every implementer brief. On verify, before/after any run that adds tests: snapshot **`%APPDATA%\bio\modlists.json` AND every seed `%APPDATA%\bio\modlists\<id>\workspace.json`**, run `cargo test --lib`, confirm all are byte-unchanged — a real-config-dir write from the test suite is a directive-grade failure (treat like a BIO-source-guard hit). **The DATA-LOSS sentinel = `modlists.json` + every seed per-modlist `workspace.json`, never `modlists.json` alone** — and byte-diff that same set when auditing ANY run that touches the persistence path, not only the test suite. (Fix-Run-1 introduced an open-path reset whose missed async/one-frame refill let a save persist an EMPTY order into a seed's `workspace.json`; the sentinel watched only `modlists.json` (intact ⇒ "clean") so the wipe went undetected until the user hit it. Fix-Run-3 then Fix-Run-4 closed the code path; this scope-widening closes the *detection* gap. User-authorized 2026-05-17.)

## Test fixtures / runtime (the mechanism)

- **Re-prep mechanism (the `-d` "Seed test modlist" button is GONE — stale doc trap):** `dev_seed::seed_demo_entry` is wired *only* in `orchestrator/stubs/home_stub.rs`, which **Phase 5 P5.T15 replaced** with the real `page_home.rs` — so `infinity_orchestrator -d` no longer surfaces a Seed button. Re-prep by driving `bio::registry::dev_seed::seed_demo_entry` through a throwaway `src/bin/*.rs` (`RegistryStore::new_default()` + `WorkspaceStore::new_for_id`; load-then-append preserves existing entries; delete the bin after) — this uses the app's own serializer so the schema is exact. Hand-editing `modlists.json` is drift-prone: verified reprs are `state:"in_progress"|"installed"` (snake/lower), `game:"BGEE"|"BG2EE"|"IWDEE"|"EET"`, RFC3339 dates, `format_version:1`. App MUST be closed first (exit-flush clobbers writes).
- Runtime config dir: `%APPDATA%\bio\`. App must be closed before linking and before swapping `modlists.json` (it loads on launch, flushes on exit).
- The **current** seed contents (which entries, which IDs) are session-edge state — captured in `.claude/orchestrator-handoff.md` at session end; re-read it at session start, do not modify mid-session.

## User working style (observed)

Tight iterate-test-fix loop; screenshots in `C:\Users\spany\OneDrive\Pictures\Screenshots\`. Reviews UX/copy closely and makes deliberate wireframe deviations (chip padding ≈7px not 4px; "click to set your name" not "(not set)"; toggle-buttons not radios; **wireframe = look authority, exact px is empirical** — `feedback_wireframe_look_not_px`) — these are recorded in SPEC as intentional so reviews don't re-flag them; preserve that pattern. Challenges unproven assertions (rightly) — verify empirically, don't reason-and-assert. Controls the discuss↔execute cadence (`feedback_discuss_then_own`): discuss the full set of points first, then expects full autonomous ownership of the agreed result — no menus, no process-as-result, no meekness, no racing ahead on point #1. Wants per-run manual-test scripts and commit+push each verified unit. Don't auto-fix the agent's flagged judgment calls — surface them.
