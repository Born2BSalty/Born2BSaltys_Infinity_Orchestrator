# Orchestrator Handoff — Live Thread

> **Role, the four principles, the run loop, the BIO-source guard, the gotchas, the fixture mechanism, and how to work with this user are NOT here** — they live in the **orchestrator skill** (`.claude/skills/orchestrator/SKILL.md`; invoke `/orchestrator` or read it first). **This file is ONLY the perishable live thread** — where the work is right now. Also distinct: `infinity_orchestrator/HANDOFF.md` = project/impl state.

## ⏸ CURRENT STATE — pick up EXACTLY here (2026-05-18)

### ⛔ SUPERSEDED — authoritative live state is the **USER-AUTHORIZED FIX ARC** block immediately below. Everything from the old "PHASE 7 COMPLETE" line downward predates this arc and is history only.

---

## ✅ USER-AUTHORIZED FIX ARC (2026-05-18, late) — ✅ **COMPLETE** (all 5 runs landed LOCAL; awaiting the user's manual test + explicit push authorization).

> **⚠ SUPERSEDED-PINS NOTICE — read this before the "PINNED root causes" paragraphs further down.** Two pins were proven FALSE in source during execution and corrected; the LEDGER below is the authoritative corrected reality, the pinned A-1/B paragraphs are kept only as the audit trail of what was originally believed. **A-1:** the pinned "defer the §13.13 `pack_meta` generation to the install-reached edge" is impossible — `export_modlist_share_code` reads `state.step3`, empty on the paste pipeline at *every* point; the user directed the real fix: **persist the code the orchestrator already has** (`set_allow_auto_install` bit-flip on the held/pasted code; never `pack_meta` on the paste path; the Workspace path still uses `pack_meta` — step3 IS populated there). **B:** the pinned "`-u` arg carve-out" is a misread — there is **no `-u` arg anywhere in BIO**; the real mechanism is BIO's own `weidu_log_mode` `log <folder>` token, reused **ZERO-BIO** (no seventh carve-out; the six stand). Do NOT re-investigate or re-apply the stale pinned A-1/B mechanisms.

### ⏱ ARC PROGRESS LEDGER (orchestrator-maintained — the authoritative corrected state)

**User cadence (explicit, 2026-05-18):** full arc authorized; "get it all done and come back to me when it's ready for my testing" → run all 5 runs autonomously, **no per-run user check-in**, commit each locally, **NO push**, return only at the integration pass (or on a genuine blocker / a decision the framework makes the user's). **T-B = DROP** (user-authorized: "Open data folder" Kebab removed). Methodology: every run gets an orchestrator-independent **runtime-instrumented trace artifact** (not tests/PNGs alone).

Orchestrator-decided run-slicing (acceptance = the SMOOTH FULL FLOW, not per-fix isolation):
1. **Clean base** (THE AMENDMENT revert + A-2 revert + T-B drop + doc-sync reversal) — ✅ **DONE & orchestrator-verified**, commit **`7339e4e`**. `weidu_component_logs` back in `<dest>`; `per_install_dirs` 3-arg pre-Fix-A; `modlist_data_dir` KEPT (workspace.json parent only); "Open data folder" removed; BIO-guard clean, both binaries no-op, `cargo test --lib` 386/0, DATA-LOSS sentinel byte-identical across own test run, runtime trace re-run confirmed dest-resolution. Orchestrator also caught + inline-fixed one missed doc-reversal (HANDOFF §13.12a cross-phase-context block).
2. **A-1 lynchpin** — user-directed resolution (2026-05-18): persist the code the orchestrator already has (the pasted code / the stored code), NOT regenerate via `pack_meta`/`export_modlist_share_code` (which reads empty `state.step3` on the paste pipeline — proven impossible). `set_allow_auto_install` decodes the held payload and flips only the bit, independent of `state.step3`. Workspace path unchanged (uses `pack_meta`; step3 is populated there). — ✅ **DONE & orchestrator-verified**, commit **`6f7f771`**. BIO-guard clean, both binaries no-op, `cargo test --lib` 392/0 (+6 temp-path tests), DATA-LOSS sentinel byte-identical, runtime trace reproduced: install-start writes held code (false form) + `install_started_at` + entry registered; C3-clean exit flips InProgress→Installed + rewrites true-bit; provenance "Tactical EET 2026" not "Shared modlist". **Doc-sync for the A-1 mechanism change deferred to the arc-end consolidated pass (SPEC §13.3/§13.13, plan P7.T3/T6/T17, HANDOFF, PENDING, live thread).**
3. **B — per-component logs written.** Pinned `-u`-arg mechanism was a misread (no `-u` anywhere in BIO; real mechanism = the `weidu_log_mode` `log <folder>` token via BIO's own `sync_weidu_log_mode`, only wired to Step-1 GUI). Orchestrator decided + verified: do it **ZERO-BIO** (reuse the existing `pub fn` from `per_install_dirs::derive_per_install_dirs`) — **NO seventh carve-out; the six stand.** — ✅ **DONE & orchestrator-verified**, commit **`1c36856`**. Guard clean, both binaries no-op on re-run, `cargo test --lib` 395/0 (+3 additive/idempotent regression tests), DATA-LOSS sentinel byte-identical, runtime trace reproduced (additive `--weidu-log-mode` w/ `log <dest>\weidu_component_logs`, single-game + EET). Orchestrator removed the implementer's acceptance-driver `pub` wrapper pre-commit (commit-hygiene; in-crate test rewired). **Doc-sync for B (SPEC §1 "no new carve-out, six stand" + §13.12 #2 mechanism = `log` token not `-u`; HANDOFF/PENDING/plan) deferred to the arc-end consolidated pass.**
4. **#1 — net-new parallel streaming downloader** — net-new `src/install_runtime/stream_downloader.rs` (bounded pool, real per-asset byte progress) replaces BIO's serial download; `arm_auto_build` no longer arms `pending_saved_log_download` (no double-download — user-approved orchestrator-side); `archive_file_name` reused read-only, `app_step2_update_extract` reused **unchanged**; 0/0 post-extract flash eliminated. — ✅ **DONE & orchestrator-verified**, commit **`3d2ffed`**. Guard clean, both binaries no-op, `cargo test --lib` 407/0 (+12), DATA-LOSS sentinel byte-identical, runtime trace reproduced: 6/6 contract proofs (parallel ≤4, monotonic per-asset bytes, exact write path, BIO-verbatim vectors, extract once, no double-download).
5. **Integration pass + consolidated doc-sync** — ✅ **DONE & orchestrator-verified**, commit **`837bef2`**. Composed end-to-end Install-Modlist-paste runtime trace (space-containing dest): **6/6 composed-flow points PASS** — the four fixes provably integrate. +1 in-crate composed test (408/0). Consolidated doc-sync across SPEC §1/§3/§4.3/§13.3/§13.12/§13.12a/§13.13 + plan P7.T3/T6/T17 + HANDOFF/PENDING/overview (orchestrator independently re-grepped, spot-read SPEC §1 accurate, fixed one missed `flip_to_installed` manifest row). Guard clean, both binaries no-op, DATA-LOSS sentinel byte-identical.

---

### ✅ ARC COMPLETE — orchestrator's job RIGHT NOW = stand by for the user's manual test. Do NOT push without an explicit ask.

**State:** all 5 runs + interleaved ledger commits **LOCAL & UNPUSHED** (HEAD `837bef2`). Tree clean. Both binaries gate-fresh no-op; `cargo test --lib` 408/0; DATA-LOSS sentinel byte-identical to the fresh reference below. The ENTIRE Phase-7 arc is local — **push remains gated on the user's explicit authorization.**

**Arc commit map (local, unpushed):** `7339e4e` R1 AMENDMENT · `6f7f771` R2 A-1 persist-held-code · `1c36856` R3 B zero-BIO log-token · `3d2ffed` R4 #1 parallel streamer · `837bef2` R5 integration+consolidated-doc-sync · interleaved `f5e8511`/`3b31e4d`/`30678fc` ledger doc commits.

**Next user inputs (three):** (1) **user reports an integration-test issue** → premise-check vs real source + the corrected mechanisms (NOT the stale pins) + confirm the binary is gate-fresh BEFORE any deep dive; if real, scope a fix-run per the skill's "How to run a run". (2) **user approves + explicitly authorizes a push** → push; then Phase 8 (`plan/phase-08-popup-reskins-polish.md`). (3) **user asks something else** → act on it; nothing about the arc is pending implementation.

**DATA-LOSS sentinel — fresh working reference (pinned baseline was stale from user manual-test drift, confirmed not a breach; re-verify byte-identity around every future `cargo test --lib`):** `modlists.json`=`C238C5DB11016CB36D553CCA16638407D4E2A6CB9E2A1424B19582B8DAF4DE31`; `KRS5ZBMT0028/workspace.json`=`3A05F47EE76839802E538FACB6D52D0BF8FF10068FCA7FE40349831336ACED1B`; `KRW34EKV007H`/`KRW3FV80004H`/`KRWY3GJC000D`/`KRWYJGKH00CX`/`KRY5HGQQ0039`/`KRYFBQJ3004Y`/`KRYJE33Z001X`/`KRYJYB8A0045`/`KRYK29XG001X` each `…/workspace.json`=`BF4541F7F1E899A226C57D4BE73AA97C621762A7DFBC55E106CC2ED2A18B5C70`; `KRW4TGJ60080/workspace.json`=`9C429FF8DCABE41471DBB9C27E1F3EDACF6B82D927E8C820E03A08B7B1A9E482`. (Still NOT the canonical 2-entry seed — stable is all the sentinel needs; the user re-preps the seed for their manual pass.)

---

**Branch/state.** `overhaul/infinity_orchestrator`, HEAD = `8df994a` — a clean LOCAL merge bringing upstream BIO fix `a38e360` "Allow spaces in per-component WeiDU log folder" (fork `main` fast-forwarded `18d2f43..a38e360`; then merged into overhaul; the −9-line removal of the no-space check in `src/core/app/state/state_validation_paths.rs`). **NOT pushed — the ENTIRE Phase-7 arc is local; push remains gated on explicit user authorization.** Tree clean. Prior local Phase-7 fix commits (all unpushed): `4a8db16` (FIX-1 download_archive preconds + FIX-2 post-success on-disk import-code true-bit rewrite), `f84fdcb` (per-install WeiDU-log SOURCE dirs — the import-arm root fix), `ffcba48` (bundled: Fix-A appdata `-u` relocation + open-data-folder + console clip + Downloading freeze/scroll), `a0bf735` (Downloading follow-up: BIO-aggregate overall bar + per-mod bar restored + footer bound), `8df994a` (this upstream merge). Per-fix detail: `infinity_orchestrator/PENDING_VERIFICATION.md` + `infinity_orchestrator/HANDOFF.md`.

**Constraints (unchanged).** CRITICAL DIRECTIVE / six carve-outs / net-new-vs-reuse; skill run loop; per-run commit (Xgatt-only, NO `Co-Authored-By: Claude`); NO push w/o explicit ask; scoped rustfmt; BIO-source guard on every staged set; DATA-LOSS sentinel (`%APPDATA%\bio\modlists.json` + every seed `workspace.json` byte-identical around any `cargo test --lib`). **HARD methodology directive (this arc):** every fix gets RUNTIME-INSTRUMENTED confirmation (a trace artifact, like the prior `PIPELINE_TRACE.log` probe that actually solved the download stall) — in-memory tests + render PNGs are NOT sufficient; A-1 is exactly the class static/in-memory verification structurally cannot catch.

**#1 — NET-NEW STREAMING DOWNLOADER (AUTHORIZED as presented; user wants our own impl).** BIO's download is a serial `ureq`+`io::copy` loop emitting only an aggregate `"Downloading updates: N/M"` — no per-asset byte signal exists (the real reason every per-mod-progress attempt was coarse). `Step2UpdateAsset` (`src/core/app/state/state_step2.rs:183-192`) carries a direct `asset_url` + deterministic filename. BIO's EXTRACT is decoupled: `app_step2_update_extract_plan.rs:48-51` re-derives `archive_dir.join(app_step2_update_download::archive_file_name(asset))` and gates on `.exists()`, not on who downloaded. Build a **net-new `src/install_runtime/` streaming downloader**: reuse `ureq` (already a dep), `Content-Length`→total, manual read-loop→bytes, bounded parallel pool (~3–4). It MUST (a) write each archive to exactly `PathBuf::from(state.step1.mods_archive_folder.trim()).join(app_step2_update_download::archive_file_name(asset))` (that fn is already `pub(crate)` + already used by `archive_store` — zero BIO edit), (b) set the EXACT `state.step2` shapes: `update_selected_downloaded_sources.push("{label} -> {dest}")`, `update_selected_download_failed_sources.push("{label}: {err}")`, `update_selected_download_running` true→false, leave `update_selected_update_assets` intact, (c) on completion call `crate::app::app_step2_update_extract::start_step2_update_extract(...)` (the same `pub(crate)` entry BIO's poll calls — same same-crate reachability the orchestrator already uses for `advance_pending_saved_log_flow`). **Arming change (the one design decision — user-approved):** stop arming BIO's `pending_saved_log_download` so BIO's serial worker doesn't ALSO fire (double download) — adjust orchestrator-owned `auto_build_driver::arm_auto_build`/`render_live` so the new module owns "resolved assets → parallel stream → result vectors → trigger extract." Orchestrator-side only; NOT a BIO edit, NOT a SPEC-CONFLICT. New orchestrator-owned `Receiver` on `OrchestratorApp` (mirror `step2_update_download_rx`, drained in `poll_step2_channels`); `DownloadProgress` gains a per-row `Option<(bytes, Option<total>)>` so `ModDownloadStatus::Downloading` carries a real fraction. Plugs in `render_live` between `archive_store::stage_known_archives` (pre) and `ingest_downloaded_archives` (post) — the boundary the redesign already owns; coexists with content-addressed dedupe (extract+ingest re-derive paths/read fs). No-Content-Length → graceful byte-count/indeterminate. Key files: `src/core/app/step2/update/app_step2_update_download.rs` (contract source), `app_step2_update_extract_plan.rs:48-51` (`.exists()` decoupling seam), `src/install_runtime/archive_store.rs:88,331-483`, `src/ui/install/stage_downloading.rs:507-806`, `src/install_runtime/auto_build_driver.rs:305-328`, `src/core/app/app_step2_saved_log_flow.rs:86-110`. **Also eliminates the post-extract 0/0 grid flash** (own the download→extract handoff; hold the last grid until the install screen takes over).

**A-1 — THE LYNCHPIN (AUTHORIZED).** Symptom: Install-from-code → no `install_started_at`, no `latest_share_code`, no `modlist-import-code.txt`, never flips Installed. Root cause: `register_and_write_install_start_artifacts` DOES run and DOES persist the entry (`install_modlist_registration.rs:303-399`; direct `registry_store.save`@:376 + `WorkspaceStore::new_for_id().save`@:359-361 — the entry IS in modlists.json; the earlier "no entry" claim was wrong). But its §13.13 sub-step `start_hooks::write_install_start_artifacts:249-258` → `share_export::pack_meta` → `export_modlist_share_code` reads `state.step3.{bgee,bg2ee}_items` (`modlist_share.rs:export_weidu_logs:485-492`) for build-from-scanned-mods, and fires in the SAME one-shot `pipeline_armed` frame as the import — BEFORE the async scan→apply-log (later frames via `poll_step2_channels`/`advance_pending_saved_log_flow:71-84`) populates step3 → `export_modlist_share_code` Errs → `pack_meta` `?`-bails before writing started/code/the on-disk file → caller swallows the Err non-fatally (`install_modlist_registration.rs:426-440`). **Fix (orchestrator-side, zero BIO):** keep the entry registration where it is; **DEFER the §13.13 artifact generation** (`write_install_start_artifacts`) until the pipeline has populated the needed state — trigger it at the install-reached edge (`start_auto_build_install`/`pipeline_reached_install`), not at arm time. Seam: `src/ui/install/stage_downloading.rs::render_live` Ok-arm ordering + the §13.13-bundle invocation point. Then FIX-2's clean-exit true-bit rewrite + the in_progress→Installed flip (`orchestrator_app.rs:maybe_flip_to_installed_on_clean_exit:598-665`, anchor `active_install_modlist_id`@`install_modlist_registration.rs:449`) have their data. **A-v** (never flips) is a knock-on — verify the clean-exit flip end-to-end with runtime instrumentation once A-1 lands.

**A-2 — AMENDED (orphan concern largely MOOTED by THE AMENDMENT).** Was: per-install dir created before import; failed import → orphan appdata `weidu_component_logs` w/o entry; id diverges across failed passes (`clear_preview` nulls `install_modlist_id`). With weidu_component_logs back in the destination (AMENDMENT), the appdata-orphan disappears. Residual hygiene only: dirs created before import (`auto_build_driver::prepare_install_dirs_and_maybe_import_with_data_dir:169-211`); on import-Err the `render_live` Err-arm (`stage_downloading.rs:684-705`) doesn't register, leaving a partial per-install dir set in the user-owned destination. Minor — add Err-arm cleanup or register-before-import for coherence if cheap, but NOT the reported defect. Single-pass id-threading is correct (only `stage_downloading.rs:586` writes `install_modlist_id`; only `clear_preview` clears it).

**A-vi — NOT A BUG (record, do not "fix").** name="Shared modlist" = correct `FALLBACK_NAME` (`install_modlist_registration.rs:167-173,109`) when the pasted code has no packed `name`. Read IS wired. Pasted codes are nameless because generated by the path A-1 breaks (ties to the still-open authoring/provenance gap). Fixing A-1 makes app-generated codes carry provenance (`pack_meta` injects name/author/forked_from). No code change to the read.

**B — BIO CARVE-OUT (USER-APPROVED, narrow).** Symptom: `weidu_component_logs` folder created but EMPTY. Root: the auto-build install runs via `mod_installer` (`core/app/step5/command_config.rs::build_install_command_config:7-49` → `core/install/step5_command_config.rs::InstallCommandConfig:5-45` → `step5_command_install.rs::build_install_invocation` + `step5_command_common_args.rs::append_common_args:12-56`), which has NO `weidu_log_folder`/`weidu_log_log_component` field and emits only `--weidu-log-mode` — the `-u`/component-log-folder arg is NEVER emitted on the install path (only `weidu_exec.rs::build_args`, the read-only SCAN path, emits `-u`). **USER-APPROVED narrow BIO carve-out:** add `weidu_log_folder` + `weidu_log_log_component` to `InstallCommandConfig`, copy them in `build_install_command_config`, emit the `mod_installer` `-u`/component-log-folder arg in `append_common_args` (or `build_install_invocation`). Exactly this arg-threading, nothing more. **Record as a new, explicitly-user-authorized (2026-05-18) Phase-7 BIO carve-out** (behavioral, not serde-default — note it in SPEC §1 carve-out list + the doc-sync). SPEC §13.12 #2 is thereby honored.

**THE AMENDMENT — `weidu_component_logs` RETURNS to the destination (REVERT Fix-A's appdata relocation).** Upstream `a38e360` (merged, `8df994a`) removed the no-space restriction that was the SOLE reason Fix A moved `-u` to `%APPDATA%\bio\modlists\<id>\`. Per explicit user instruction: `weidu_component_logs` = `<destination>/weidu_component_logs` again (original §13.12 #2 / §13.12a per-install-inside-destination). Fresh agent: (a) revert the `per_install_dirs.rs` Fix-A change so `weidu_component_logs = dest.join(WEIDU_COMPONENT_LOGS_DIRNAME)` and `step1.weidu_log_folder` points there; remove the `modlist_data_dir`-routing of `-u`; WeiDU-log SOURCE dirs unaffected (always in dest; f84fdcb stands); (b) keep `modlist_data_dir` (`store_workspace.rs`) as the canonical `workspace.json` parent; (c) **T-B "Open data folder":** appdata dir now holds only `workspace.json` (no weidu_component_logs) — reconcile T-B + its SPEC §3 doc-sync; per-install logs/artifacts are in the destination (via "Open install folder"); decide whether "Open data folder" stays a distinct Kebab item or folds in (flag to user if non-obvious); (d) **doc-sync REVERSAL:** undo this session's SPEC §13.12/§13.12a/§3 + plan P7.T17 + HANDOFF/PENDING edits that moved `-u` to appdata + cited the no-space rationale — `weidu_component_logs` is per-install-in-destination again; record upstream `a38e360`/merge `8df994a` as why the constraint+relocation are gone (state_validation_paths.rs no longer has the no-space check).

**THE INTEGRATION REQUIREMENT (user-emphasized — the actual goal).** A-1, A-2(amended), B, #1 must flow TOGETHER so the end-to-end UX is smooth and every artifact is correct: Install → choose folder + paste → preview → **downloads (real per-item byte bars, parallel — #1)** → installation → complete, with along the way: registry entry created+persisted with correct name/provenance, per-install dirs in the destination, per-component logs ACTUALLY written (B), `modlist-import-code.txt` written at start then rewritten true-bit on clean exit (A-1 + FIX-2), state flipped in_progress→Installed on clean exit, no 0/0 flash. Slice into runs as the implementer sees fit (suggested: AMENDMENT + A-2 revert first = clean base → A-1 → B carve-out → #1 downloader → #4 §4.4 banner) but **acceptance is the SMOOTH FULL FLOW, runtime-instrumented, not per-fix in isolation**. #4 (§4.4 success banner) rides behind A-1 (needs a real entry/name) — small.

**Run loop:** orchestrator skill "How to run a run" — dispatch plan-implementer (scoped) → independently verify ALL gates incl. the runtime-instrumentation directive → scoped rustfmt → reconciled per-run commit (Xgatt-only, no Claude trailer) → NO push (gated) → doc-sync incl. THE AMENDMENT reversal. This block carries the full pinned scope so the executing agent does NOT re-investigate.

---

### ✅ PHASE 7 COMPLETE — awaiting the user's one-pass verification + push authorization. NOTHING is in flight; do NOT dispatch or re-run anything.

**Read order for a fresh agent:** the orchestrator skill (`/orchestrator`) → this block → `infinity_orchestrator/PENDING_VERIFICATION.md` (the per-run + final-fix Phase-7 checklists) → `infinity_orchestrator/HANDOFF.md` (impl state). The skill is the timeless *how*; this is the current *where*.

**What's done.** Phase 7 (Step-5 install runtime) is fully implemented, **independently orchestrator-verified, and committed locally** — the autonomous arc the user authorized 2026-05-18. 5 runs + a behavior-neutral helper extraction + the user-decided final §13.13/registry-lifecycle fix ("Full correct fix", resolution A). SPEC §13.13 is met for every install entry point; the broader Install-Modlist-paste registry-lifecycle gap is closed. Every run: zero BIO source, BIO-guard clean, DATA-LOSS sentinel byte-identical, both binaries build clean, render PNGs personally judged, every PLAN-GAP/SPEC-CONFLICT premise-checked vs source and the docs corrected. The **mandatory end-of-arc no-op rebuild gate is GREEN** (a confirmed second-consecutive `Finished` with no `Compiling bio` — the launched binary is provably current with HEAD).

**Branch / commits.** `overhaul/infinity_orchestrator`; **last code commit = `accb66f`** (the Phase-7 final fix — `git log` for the map below); HEAD = that + trailing **doc-only** commits (this handoff rewrite); **NOT pushed** (the whole arc is local — push is pending the user's explicit authorization; this repo has documented push-sensitivity, never push without an explicit ask). Tree clean. (The old `overhaul/infinity_orchestrator-p7` branch is DISREGARDED — Phase 7 built in this branch.) Operative directive set = the orchestrator skill + the `feedback-*` auto-memories incl. `feedback-ui-render-gate`.

**The orchestrator's job RIGHT NOW = stand by.** Do **not** dispatch a run, do **not** re-verify Phase 7, do **not** push. Three possible next user inputs:
1. **User reports a Phase-7 verification issue** → premise-check it against real source/plan/wireframe + confirm the binary is gate-fresh (per the skill's stale-binary gotcha) BEFORE any deep dive; if real, scope a fix-run per the skill's "How to run a run" (dispatch → independently verify all gates incl. the render gate + the DATA-LOSS sentinel below → reconciled local commit → doc-sync). Same loop as the arc.
2. **User approves Phase 7** → if/when they explicitly authorize a push, push; then the next phase is **Phase 8** — plan `infinity_orchestrator/plan/phase-08-popup-reskins-polish.md` (full-fidelity reskin sweep + residual flag policies #6/#7 + dot background/toasts/hover/clipboard + the deferred Create-cleanup/§2 visual items). Slice it into runs and run the same loop.
3. **User asks to push / something else** → act on it; nothing about Phase 7 is pending implementation.

**User's remaining (designed) step — theirs, not the orchestrator's:** re-prep the canonical 2-entry test seed FIRST (the `%APPDATA%\bio` state drifted off-seed during testing — the precondition + mechanism are in `PENDING_VERIFICATION.md` / the skill's "Test fixtures / runtime"), then the one-pass: the `PENDING_VERIFICATION.md` per-run + final-fix checklists + phase-07 Verification 1–11 + the C3/C5 checks. Orchestrator-verified ≠ the user's visual/functional sign-off — that human pass on a gate-fresh binary is the part only they can do.

**Phase-7 commit/audit map (local, unpushed; full per-run detail in `PENDING_VERIFICATION.md` + commit messages):**
- doc-sync `4464634` (verification-plate clear) · arc-setup `f244496`
- **R1** `5b8a60e` — P7.T1/T2/T8 Step-5 spine + chrome + Previous-lock
- **R2** `e02dbe9` — P7.T3/T9/T9b/T16/T14 install-start + `pack_meta` + concurrency + C5 rail-lock + #1/#5 flags + statusbar (carve-out #5 intact)
- **R3** `0b58cc9` — P7.T4/T5/T6/T7/T12/T13 success banner + post-install + `flip_to_installed` + async size worker + share dialog (C3 flip fires once)
- **R4a** `4e1d064` — P7.T17 live download/extract/import pipeline + per-install dirs + content-addressed staging (engine COMPOSED not forked; the only `remove_dir_all` verified safe)
- **R4b** `dabc749` — P7.T15 real §4.4 install screen + P7.T10 Reinstall + P7.T11 matrix
- `7e0e414` — behavior-neutral `write_install_start_artifacts` extraction (on_install_start byte-identical)
- `222f492` — decision-record (user chose "Full correct fix")
- **final fix** `accb66f` — `install_modlist_registration` (Install-paste registers an in-progress entry + §13.13 post-import for paste & Reinstall + `active_install_modlist_id` clean-exit flip) — **PHASE 7 COMPLETE**

**Phase-8 propose-only follow-ups (tracked, NOT done — recorded so they aren't lost):** (a) dedupe `src/ui/shared/format_relative.rs::format_install_duration` vs `src/ui/shell/shell_statusbar.rs::format_elapsed` (parallel Duration→clock formatters); (b) the §4.3 Downloading grid's source/status columns collide at ~1045 px (Phase-5-chassis column-width responsiveness — the live wiring is correct). Both are in `plan/phase-08-popup-reskins-polish.md`'s deferred backlog area too.

**Standing process gotchas (arc-proven — apply to any future fix-run / Phase 8; also in the skill):**
- **Scoped rustfmt = `rustfmt --edition 2024 <LEAF .rs files only>`.** `--skip-children` is NOT a valid flag in this rustfmt (errors `Unrecognized option`). Passing a module-root (`lib.rs`, any `mod.rs`) recurses the whole tree. Pass only true leaf files (inline `#[cfg(test)] mod tests {}` is fine — no recursion); EXCLUDE `lib.rs`/`mod.rs` roots. The post-rustfmt staged-set reconcile-and-revert (`git checkout -- <out-of-scope file>`) is the real safety net.
- **Windows UAC footgun:** a freshly-built test/exe whose binary name contains `install`/`setup`/`update`/`patch` triggers Windows Installer Detection → `os error 740`. Name render-gate tests `ui_snapshot_<feature>.rs` avoiding those tokens.
- **`git add` on Windows churns `.rs` mtimes** ⇒ a post-stage commit-build may show one spurious `Compiling bio` with no code change. The freshness proof is a **second consecutive** `cargo build --bin infinity_orchestrator --release` that is a true no-op (`Finished`, no `Compiling bio`). This IS the end-of-arc gate; re-use it after any future doc/commit step.
- **DATA-LOSS sentinel (directive-grade) — STILL byte-identical; re-verify around ANY future `cargo test --lib`** (a Phase-7 fix-run, Phase-8, anything): byte-diff `%APPDATA%\bio\modlists.json` + EVERY seed `%APPDATA%\bio\modlists\<id>\workspace.json` before/after; any change = ABORT (a test bound the real config dir — repoint stores to `std::env::temp_dir()`). Baseline (unchanged across the entire Phase-7 arc through multiple real registry/workspace writes):
  - `modlists.json` = `3D8212D7E4416606BF17DB69F6B49B655BA65CE8DB875133DCC0F6DDD07C7B12`
  - `KRS5ZBMT0028/workspace.json` = `AE702C9AF1FAEC79785653125D7F5699742BE24B2D311210816D0F9CAFE1E959`
  - `KRW34EKV007H` / `KRW3FV80004H` / `KRWY3GJC000D` / `KRWYJGKH00CX` each `…/workspace.json` = `BF4541F7F1E899A226C57D4BE73AA97C621762A7DFBC55E106CC2ED2A18B5C70`
  - `KRW4TGJ60080/workspace.json` = `9C429FF8DCABE41471DBB9C27E1F3EDACF6B82D927E8C820E03A08B7B1A9E482`
  - (NOT the canonical 2-entry seed — stable, which is all the sentinel needs; the user re-preps the canonical seed for their manual pass.)

> 🗄️ **PRE-ARC AUDIT BELOW — SUPERSEDED by the "✅ PHASE 7 COMPLETE" block ABOVE. Do NOT action anything from here down as "next"** (it predates the 2026-05-18 Phase-7 arc: the Phase-6 close + the *original* Phase-7 prep/run-slicing + stale push refs). Kept only as the historical record of how Phase 7 was planned. The authoritative current state is the block above.

### ✅ Closed (Phase-6 + post-completion hardening)
DATA-LOSS workspace.json order-wipe regression CLOSED (Fix-Run-3 `0b9d53d` + Fix-Run-4 `28d9975`, user-confirmed). Create #4 wireframe-alignment cluster fixed (Fix-Run-5 `600dbb3` + Fix-Run-6 `e305f92`) — P1–P5 orchestrator-render-verified at 1280/1045/1021/960 × scratch/import; pending only the user's one-pass live confirm. `egui_kittest` render gate committed (`7086149`) + standing rule. #8 forked share code user-confirmed end-to-end. SKILL.md DATA-LOSS sentinel hardened (user-authorized). 6 Phase-6 verification items user-confirmed (input border, nav indicator, Step-3 hints, glyphs, load-draft delete, rail). Detail: `overview.md` 2026-05-18 revision log.

### ⏳ Deferred to Phase 8 (do NOT fix in Phase 7)
5 minor Create-screen issues + the box-equal-height problem (documented in `plan/phase-08-popup-reskins-polish.md` with screenshots, user 2026-05-18); the save-model UX redesign; the deselect-last/single-component edit-loss; the orphaned `workspace_nav_bar::forward_primary_button` dead-code; promote the render gate to `try_snapshot` golden baselines.

### 🗄️ [SUPERSEDED — Phase 7 is COMPLETE; see the block at the top. DO NOT dispatch Run 1.] Original pre-arc Phase-7 prep + run-slicing (audit only)
Plan = `infinity_orchestrator/plan/phase-07-install-runtime.md` (P7.T1–T17 + Verification 1–11). Governing constraints (all defined in that doc — read it first): the **C3 clean-exit triple** (`install_running==false && last_exit_code==Some(0) && last_install_failed==false`), the **C5 rail-nav lock**, **H8** (no registry_snapshot), **H9** (post-install row immediately ABOVE BIO's panel), and "compose around `bio::ui::step5::page_step5::render`, never edit BIO Step 5". Per-run loop = orchestrator skill "How to run a run" (dispatch plan-implementer bg → independent verify INCLUDING the `egui_kittest` UI render gate for any redesign-UI run → scoped rustfmt → reconciled commit per run → doc-sync [offload to the agent, accuracy-grep] → final no-op rebuild gate). **Commit at the END of EACH run. The user verifies the whole phase in ONE pass at the end** (phase-07 Verification 1–11 + C3/C5). Keep orchestrator context lean — offload doc-sync to the implementer each run.

**Run 1 — Step-5 runtime spine + workspace chrome (pre-install).** P7.T1 (`OrchestratorApp` owns `step5_terminal`/`step5_terminal_error`/`step5_console_view`/`step5_prep_rx`/`step5_pending_start` + the 6 Step-2 receivers; register `mod install_runtime` in `src/bin/infinity_orchestrator.rs`), P7.T2 (`src/ui/workspace/step5/` net-new — `page_workspace_step5::render` wraps `bio::ui::step5::page_step5::render`; replace the Phase-6 Step-5 stub; banner + post-install rows are empty slots pre-install), P7.T8 (workspace `← Previous` nav lock). Breakpoint: in-workspace Step 5 shows BIO's full embedded panel; Previous disabled post-Install-click; both binaries build; `cargo test --lib`; UI render gate covering the new Step-5 chrome. No BIO Step-5 edit.

**Run 2 — Install start + concurrency + rail-lock (C5) + flag policies + statusbar.** P7.T3 (`install_runtime/{start_hooks,import_code_writer,registry_transition}` + **net-new `src/registry/share_export.rs::pack_meta`** — SPEC §13.3 generation: zlib+base64url+serde_json round-trip, existing deps, composes `bio::app::modlist_share::export_modlist_share_code`, NEVER patches BIO [carve-out #5 "generation is not a BIO modification"]; `allow_auto_install=false` at start; variant-gated import-code write §13.13), P7.T9 (`install_concurrency::install_in_progress` per-button gate), P7.T9b (C5 rail lock — `install_runtime/rail_lock_reason.rs` + edit `src/ui/orchestrator/nav_rail.rs` to honor `rail_locked: Option<&RailLockReason>` + compute once/frame in `OrchestratorApp::update`), P7.T16 (`flag_policies::compute_flags` #1 `-s`/`-c`, #5 `--download`), P7.T14 (`shell_statusbar` → `1 job running · <modlist> · <elapsed>`). Breakpoint: Install click → `modlist-import-code.txt` + `install_started_at` written, concurrency gate + rail disabled (C5 §13.15 tooltip; `populate_wizard_state_from_workspace` NOT called mid-install), statusbar running; fresh Create→New install kicks BIO's pipeline (full share-code-workflow download = Run 4).

**Run 3 — Post-install: banner + actions + registry flip + share dialog.** P7.T4 (`success_banner` gated on the C3 triple; `format_install_duration` in `src/ui/shared/format_relative.rs`), P7.T5 (`post_install_actions` row immediately ABOVE BIO's panel per H9 + flip `workspace_header` Share-import-code enabled), P7.T6 (`registry_transition::flip_to_installed` → Installed + install_date + counts + **regenerate `latest_share_code` via `share_export::pack_meta` with `allow_auto_install=true`** + async size worker, `total_size_bytes=None` then 2nd atomic write), P7.T7 (`share_paste_code_dialog`), P7.T12 + P7.T13 (verify only, no edit). Breakpoint: clean install → banner + post-install row + registry→Installed + Home Installed chip + share dialog; C3-fail stays in-progress; cancel paths.

**Run 4 — Live download/import pipeline + Install-Modlist stage 4 + Reinstall.** P7.T17 (`install_runtime/per_install_dirs.rs` [per-install Mods + §13.12 #3/#4 clone dirs + #2 weidu_component_logs dir INSIDE the destination — install-critical, NOT Phase 8], `install_runtime/archive_store.rs` [content-addressed staging wrapping `bio::app::app_step2_update_download`/`_extract`, NO BIO edit], `install_runtime/auto_build_driver.rs` [drive `import_modlist_share_code` + saved-log/auto-build via `app_update_cycle::poll_before_render` with orchestrator-owned receivers], wire Phase-5 `src/ui/install/stage_downloading.rs` live; escalate `SPEC CONFLICT` if reuse blocked — do NOT fork the engine), P7.T15 (`src/ui/install/stage_installing.rs` real — wraps `page_step5::render`, own minimal chrome), P7.T10 (`reinstall_route` + `reinstall_route_wire` + `pending_reinstall_id: Option<String>` on `OrchestratorApp`; registry flips Installed→InProgress at Install-click, not Reinstall-click), P7.T11 (import-code write matrix end-to-end — Resume skips/checksum-equal, others overwrite). Breakpoint: Install Modlist paste→preview→**live downloading**→installing→success; Reinstall round-trip; import-code matrix. **T17 is the largest task — the implementing session MAY sub-split Run 4 (4a=T17 pipeline, 4b=T15+T10+T11) if context-bound.**

**After Phase 7:** user one-pass verification → Phase 8.

> _Older blocks below are pre-this-session history — superseded by the block above; kept for audit, do not action them as "next"._

## Pending user verification

`infinity_orchestrator/PENDING_VERIFICATION.md` — **Phase-6 verification plate CLEARED (user, 2026-05-18).** #1 Workspace Step-2 alignment + #3 workspace rename-pencil + #4 Step-3 C4 chrome user-verified → Confirmed closed. FR2-4 (Create full one-pass) + #2 (residual grey bar under the GameTab strip = reused-BIO content-pane grey top — Phase-8 carve-out #6 grey-pane-border surface) punted to the **Phase-8** verification one-pass; the Phase-6 fixes are shipped and NOT reverted, only the live sign-off moves. **No open Phase-6 verification items.** Next user verification event = the **Phase-7** phase-end pass (`phase-07-install-runtime.md` Verification 1–11 + C3/C5) — do not test until the orchestrator signals Phase 7 complete.

## Active phase work order

`infinity_orchestrator/plan/phase-06-create-workspace-shell.md` — the active phase work order (Phase 6 in progress: R1/R2 + Step-3 C4 P6.T2d shipped; **Run 3 = P6.T7/T9/T13/T14 dispatched 2026-05-17**; Run 4 = P6.T8 fork + P6.T11 dirty-bit + P6.T15 nav-flush). (Update this pointer as phases advance.)

## Where the thread is

Branch `overhaul/infinity_orchestrator`. Last **pushed** = **`179123d`** (origin: `github.com/Xgatt/Born2BSaltys_Infinity_Orchestrator`; `bak` was a mis-tracking accident removed 2026-05-16, `remote.pushDefault=origin` pins it). **HEAD = LOCAL, unpushed** — `577961f` (step-5 rebuild-gate + Windows file-lock gotcha hardening) + the 2026-05-17 Run-2b-false-alarm doc correction on top; **push pending user authorization** (auto-mode classifier blocked it as a presumed main-branch push — it is a routine feature-branch push). Tree clean. Phases 1–4 done. Phase 5 COMPLETE (adversarial-reviewed + QA-resolved; live download → Phase 7 P7.T17 per SPEC §13.12a).

**Phase 6 IN PROGRESS — user-approved 4-run slice:** R1 spine · R2 Step-4 C4 + header/rename/save-draft/game-tabs · R3 Create + Load Draft · R4 fork + dirty-bit persistence + nav-away flush.

- **R1 DONE + user-accepted** — workspace spine + the Step-2 **C4 chrome wrapper**, hardened across Runs 1b→1e (`150593f`…`17b1dcb`): net-new redesign Step-2 chrome per wireframe (NO BIO `render_controls`/`render_tabs`/`render_header`/`page_step2`/`frame_step2` — grep-verified every run); Step-2 channel poll; bounded/clip layout; native GameTab; Step2→Step3 nav-edge sync; rescan-reconcile (SPEC §6.3); first-step `← Previous`→Home (SPEC §2.2); Cancel-while-scanning; Select-via-WeiDU-Log §6.10 danger-confirm + non-destructive cancel; dev-scan (production Rescan inert pre-Phase-7 §13.12a).
- **R2 DONE + orchestrator-verified** — `6df87f9` (P6.T2b Step-4 C4 renderer — `page_step4::render` NOT called, C4 boundary holds; P6.T5 `workspace_header` + `operations_rename` REGISTRY-ONLY per §2.2; P6.T6 save-draft; P6.T10 game-tabs) → `301bf72` data-loss follow-up (a Run-2 test clobbered the real `modlists.json` on every `cargo test`; fixed test-only, **recurrence-guarded in Gotchas — directive-grade**, seed restored) → `4950b19` Run-2b (#1 cold-resume Step-2 restore via `step2_resume_scan` re-triggering BIO's cache-fed scan; #2 pencil glyph fixed; #4 verified wireframe-faithful) → `ee993a7` Run-2b #3 (Step-4 WeiDU line = the wireframe/§6.7 3 hues: amber `#d4a35c` / dark-blue `#2f6fb7` / `redesign_success`). 231 lib tests; guard empty; Step-2/1b-1e/Run-2 not regressed; both binaries link clean.

**Full per-run detail (root causes, premise-checks, judgment calls) = `infinity_orchestrator/plan/overview.md` 2026-05-16 + 2026-05-17 revision log.**

### ⏸ CURRENT STATE — pick up exactly here

**Phase 6 Run 2b re-test PASSED — user-confirmed end-to-end on a correctly-built binary. This IS the "good, next run" acceptance → next dispatch = Phase 6 Run 3.**

**Post-mortem (2026-05-17) — the reported "major regression" was a STALE BINARY, not a code defect.** The prior session handed off the Run-2b re-test against an `infinity_orchestrator.exe` that predated the Run-2b fixes (proven stale at this session's start: `cargo build --release` did a 47s `Compiling bio`, not a no-op). The user's "cold resume broken / rename doesn't persist / save-draft writes nothing / modlists.json minified" was entirely that stale artifact. Rebuilt from HEAD + **gate-confirmed true no-op**, the user re-ran the exact flow — **Select-BGEE-via-WeiDU.log + Select-BG2EE-via-WeiDU.log → save draft → ✎ rename → quit → relaunch → resume → mods + components + Step 3 order ALL restored** — flawlessly, full round-trip (a *broader* validation than the dev-scan path the old re-test script described). Run-2b code was correct in source the whole time. The deep "four silent-failure points / `registry_error` kill-switch / hollow-test" diagnosis chased a ghost the stale binary created — **retracted**; the order-source / failure-point forks are MOOT and closed. (Latent, non-blocking, NOT Run-2b bugs: save-draft/rename can no-op without user feedback = future UX polish; the 32 MiB debug-scan stack overflow is BIO-protected dev-ergonomics, off-scope.)

**Hardening shipped + proven by this exact incident:** `577961f` + the doc-correction commit on top (both **LOCAL — push was blocked by the auto-mode classifier misreading a feature-branch push as a main-branch push; pending user authorization**): step-5 mandatory final pre-handoff rebuild gate (confirmed no-op `Finished`, no `Compiling bio`, AFTER doc commits) + the Windows file-lock gotcha rewrite. Auto-memory `feedback-auto-rebuild` reframed to bind the non-editing orchestrator. **The gate already caught a real staleness this session — it works.**

**Before the next test cycle:** the user's `%APPDATA%\bio\modlists.json` is mutated off the documented 2-entry seed (`demo-modlist-2-test`/EET/3/7). Corrupt copy preserved as `%APPDATA%\bio\modlists.json.corrupt-evidence-20260516-235903` (+ the matching `workspace-…-evidence-…json`). Re-prep to the canonical 2-entry seed (the skill's "Test fixtures / runtime" mechanism) for clean Run-3 testing.

**Next dispatch — Phase 6 Run 3 (Create screen + Load Draft dialog + routing)** per the 4-run slice, following the skill's `## How to run a run` verbatim — esp. the **MANDATORY "Already built / cross-phase context" block** (Settings→Paths globals / no global mods folder / §13.12a / the Step-2+Step-4 C4 precedent / reuse Phase-5 ForkInfoPopup), the **directive-grade test-hygiene rule** (NO unit test may keep a `new_default()` store; verify via the sentinel-`modlists.json` byte-diff), **and the step-5 final pre-handoff rebuild gate**. Read `plan/phase-06-create-workspace-shell.md` P6.T7 (stage_choose) / P6.T8 (fork sub-flow — Run 4, NOT Run 3) / P6.T9 (Load Draft) / P6.T13 (wire Create into page_router) / P6.T14 (resume routing) — Run 3 = the Create + Load-Draft entry path, NOT the fork sub-flow (that's R4). If issues are reported: premise-check each against the actual code/plan/wireframe **and first confirm the binary under test is gate-fresh** before any deep dive.

> **Thread-staleness note (2026-05-17):** the blocks above predate this session's work (the `9b5b9d5` doc-sync, the Issue-1 alignment + uniform GameTab up to commit `ab4453b` — the Step-3 C4 doc-cascade in `ab4453b` was RETRACTED 2026-05-17 (premature/false-done; see overview 2026-05-17 §3) and Step-3 C4 is now a pending dispatched run — and the orchestrator-skill split). HEAD/“next dispatch” here are NOT current. Refresh this whole live-thread section before the next run; the authoritative recent record is `infinity_orchestrator/plan/overview.md` 2026-05-17 revision log. (Preserved verbatim rather than silently rewritten — updating thread state was out of scope for the skill/handoff split.)

| Run | Scope | Status |
|---|---|---|
| 1 | Home — visual + nav (P5.T1-6, T8 widget, T15 + shared widgets) | ✅ done + 4 follow-ups |
| 2 | Home — actions live (P5.T7 delete, T16 toast, T17 open-folder, T18 reinstall stub) | ✅ done + 1 follow-up |
| 3 | Install — shell + paste stage + stage-4 stub (P5.T9, T13, T14) | ✅ done + 3 follow-ups |
| 4 | Install — Preview parse + 6 tabs + carve-out #5 provenance trio + `ForkInfoPopup` (P5.T10, T11) | ✅ done — independently verified (BIO-guard clean, carve-out = exact 5 items, 178 lib tests, both binaries build); +1 follow-up: paste-code box capped to footer + scrolls internally |
| 5 | Install — Downloading §4.3 chassis (P5.T12) | ✅ done — verified (BIO-guard empty, 187 lib tests, both binaries build); live download data + per-install dirs + content-addressed staging deferred to Phase 7 P7.T17 (SPEC §13.12a). +QA follow-ups: paste-stage requires a valid destination folder (SPEC §4.1 amended) + `DestinationNotEmptyWarning` legibility fix |

Run-slicing rationale + per-run breakpoints are in session history; the canonical task list is the phase doc.

### Runs 4–5 — ✅ DONE. Phase 5 COMPLETE; Phase 6 (Create + Workspace shell) is the next dispatch.

**Run 4 outcome (independently verified by the orchestrator, not the agent's word):** BIO-source guard clean (only the authorized `modlist_share.rs`); the carve-out #5 diff is *exactly* the 5 enumerated items + the gate-required `#[cfg(test)]` module, purely additive, `export_modlist_share_code`/private envelope untouched; `cargo test --lib` 178/0 (163 baseline +15 Run-4 incl. serde-default round-trips); both binaries build; `stage_preview.rs`/`fork_info_popup.rs` spot-read SPEC-faithful. Surfaced judgment calls: a `cargo fmt` scope-creep the agent fully reverted (orchestrator-confirmed clean) → standardized scoped-fmt on `rustfmt --edition 2024 <leaf files>`; pre-existing edition-2024 rustfmt debt in HEAD redesign code (out of scope); honest parse-error render + `pub(crate)` API scoping (within spec intent); open question **D** (Overview "Mods" value) resolved later in `32781d2` (derive). The detailed authorized-scope brief below is retained as the audit record of what Run 4 was permitted to touch.

**Run 5 outcome (independently verified):** BIO-source guard empty (zero BIO files; only `src/ui/install/*`), `cargo test --lib` 187/0 (178 +9), both binaries build, `stage_downloading.rs` spot-read = the §4.3 chassis. The agent correctly **escalated a `SPEC CONFLICT`** instead of reimplementing/forking BIO's download engine — but two of its premises were wrong and the orchestrator caught them on the premise-check: it is **not** "BIO can't" (the import→auto-build pipeline exists and is reachable) and **not** "Install lacks game paths" (paths are global in Settings → Paths §11.2 and reach the owned `WizardState` via `sync_paths_from_settings`). Resolution (user, final authority): the spec hadn't defined the per-install/global directory model + content-addressed archives + the pipeline-reuse contract → added as SPEC **§13.12a**; Phase 5 ships the chassis, live wiring is **Phase 7 P7.T17**. See overview.md 2026-05-16 revision log.

**Phase 5 post-completion adversarial-review QA (2026-05-16, commit `32781d2`).** Read-only `Plan` agent scoped to Phase 5 impl + the spec/plan cascade (`df6b78a..HEAD`), governed by `spec-authority.md`. Verdict: directive-clean, zero PR#11-style hollowness, carve-out #5 exactly the authorized surface, doc cascade complete, wireframe authority not inverted, 187 tests substantive. Findings, all resolved in `32781d2`: **SC-1** Install Preview Overview `Mods` `—` vs SPEC §4.2 — user chose *derive*; net-new `install/preview_counts.rs` reusing public `Component::parse_weidu_line` (ZERO BIO edit). **M-1** dead Home `Rename` kebab — *keep + record* (SPEC §3.2 + phase-05 P5.T2 + HANDOFF). **H-1** stale `SPEC CONFLICT` banners reworded. **L-1** ForkInfoPopup collapse-chevron Phase-8 deferral cross-referenced. Verification: 189/0 tests, both binaries build, BIO-source guard empty.

---

#### Run 4 authorized-scope brief (historical / audit record)

Run 4 was **the only BIO-source touch in all of Phase 5**: carve-out #5 on `src/core/app/modlist_share.rs`, now the **provenance trio + `allow_auto_install`** (user-directed spec change 2026-05-15 — see SPEC §1 "Modlist-share provenance application", §13.3 Provenance/Generation, §10.9; overview.md revision log). The **exact and only** authorized BIO edits (P5.T10 enumerates them):
1. `ModlistSharePayload` (`#[derive(Deserialize)]`): `#[serde(default = "default_true")] allow_auto_install: bool` + `#[serde(default)]` `name: Option<String>`, `author: Option<String>`, `forked_from: Vec<ForkAncestor>`.
2. New `fn default_true() -> bool { true }`.
3. New `struct ForkAncestor { name: String, author: String }` with derive **`#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]`** — the full set so Phase 6's `ModlistEntry.forked_from` reuse needs no follow-up BIO edit; within carve-out #5.
4. The symmetric fields on `ModlistSharePreview` (`pub(crate)`).
5. Four `share_preview()` propagation lines (payload→preview, one per field).
Nothing else — all `#[serde(default)]`, zero behavior change for today's BIO. **Generation (`pack_meta`) is NOT Run 4** and is never a BIO edit: net-new orchestrator sibling (`registry::share_export`, Phase 6/7) composing `export_modlist_share_code` + a standard envelope round-trip. Run 4 is **consume-only**.

## Current test seed (live state)

Seed registry for non-empty Home/Install/Workspace testing: `C:\Users\spany\AppData\Roaming\bio\modlists.json`. As re-prepped 2026-05-16: **2 entries** — `RUN1BBBBBBBB` "Polished BG2EE" (`installed`) + `KRS5ZBMT0028` "demo-modlist-2" (`in_progress`, BGEE, 9 mods · 136 components · paused Step 3, + its `modlists/<id>/workspace.json`). The in-progress one is what Home-`resume` breakpoints need; both exercise the empty-`destination_folder` delete guard. (Restored 2026-05-16 after the Run-2 test-clobber data-loss bug — commit `301bf72`.) **Note:** mutated off-seed during the 2026-05-17 session's testing; re-prep before the next clean cycle (mechanism = the skill's "Test fixtures / runtime").
