/target
/backup/
/TODO.md
/diagnostics/
/vendor/lapdu-parser-rust-master/tests/

.vscode/
.idea/

.DS_Store
Thumbs.db
*.log
*.tmp
*.bak
*~

Push\ Github.bat

# Infinity Orchestrator redesign — generated / regenerable / archive artifacts
infinity_orchestrator/wireframe-preview/node_modules/
/Infinity Orchestrator Wireframe*.html
/infinity_orchestrator.zip

# Per-dir CLAUDE.md / AGENTS.md notes + the shared bits of `.claude/`
# (rules, agents, etc.) are tracked on this private fork so AI orientation
# travels with the repo across dev machines. Only the local harness state
# below is excluded — it's per-machine and carries permission grants /
# transient session data that shouldn't sync.
.claude/settings.local.json